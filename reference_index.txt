================================================================================
EXHAUSTIVE WEB DEVELOPMENT REFERENCE INDEX (HTML, CSS, JAVASCRIPT)
================================================================================

Total Entries Cataloged: 659
--------------------------------------------------------------------------------
• HTML Elements & Attributes : 318 entries
• CSS Styles & Properties    : 196 entries
• JavaScript APIs & Methods  : 145 entries
================================================================================

TABLE OF CONTENTS:
1. HTML REFERENCE INDEX
2. CSS REFERENCE INDEX
3. JAVASCRIPT REFERENCE INDEX

================================================================================
1. HTML REFERENCE INDEX (318 Elements & Attributes)
================================================================================

[Category: Attributes] (99 entries)
--------------------------------------------------------------------------------
▶ `a:link`
  Description: HTML attribute related shortcut
  Syntax:      <a href="http://"></a>
  VSCode/Emmet: Inside an opening tag, start typing `a:link` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ `a:mail`
  Description: HTML attribute related shortcut
  Syntax:      <a href="mailto:"></a>
  VSCode/Emmet: Inside an opening tag, start typing `a:mail` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ `img:srcset`
  Description: HTML attribute related shortcut
  Syntax:      <img srcset="" src="" alt="">
  VSCode/Emmet: Inside an opening tag, start typing `img:srcset` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ `input:checkbox`
  Description: HTML attribute related shortcut
  Syntax:      <input type="checkbox" name="" id="">
  VSCode/Emmet: Inside an opening tag, start typing `input:checkbox` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ `input:email`
  Description: HTML attribute related shortcut
  Syntax:      <input type="email" name="" id="">
  VSCode/Emmet: Inside an opening tag, start typing `input:email` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ `input:number`
  Description: HTML attribute related shortcut
  Syntax:      <input type="number" name="" id="">
  VSCode/Emmet: Inside an opening tag, start typing `input:number` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ `input:password`
  Description: HTML attribute related shortcut
  Syntax:      <input type="password" name="" id="">
  VSCode/Emmet: Inside an opening tag, start typing `input:password` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ `input:placeholder`
  Description: HTML attribute related shortcut
  Syntax:      <input type="text" placeholder="">
  VSCode/Emmet: Inside an opening tag, start typing `input:placeholder` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ `input:radio`
  Description: HTML attribute related shortcut
  Syntax:      <input type="radio" name="" id="">
  VSCode/Emmet: Inside an opening tag, start typing `input:radio` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ `input:submit`
  Description: HTML attribute related shortcut
  Syntax:      <input type="submit" value="">
  VSCode/Emmet: Inside an opening tag, start typing `input:submit` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ `input:text`
  Description: HTML attribute related shortcut
  Syntax:      <input type="text" name="" id="">
  VSCode/Emmet: Inside an opening tag, start typing `input:text` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ `link:css`
  Description: HTML attribute related shortcut
  Syntax:      <link rel="stylesheet" href="style.css">
  VSCode/Emmet: Inside an opening tag, start typing `link:css` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ `link:favicon`
  Description: HTML attribute related shortcut
  Syntax:      <link rel="shortcut icon" href="">
  VSCode/Emmet: Inside an opening tag, start typing `link:favicon` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ accesskey
  Description: Defines a keyboard shortcut to activate/focus the element.
  Syntax:      <button accesskey="s">Save</button>
  VSCode/Emmet: Inside an opening tag, start typing `accesskey` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ action
  Description: The URL a <form> submits its data to.
  Syntax:      <form action="/login">
  VSCode/Emmet: Inside an opening tag, type the Emmet snippet `form` and press Tab/Enter to insert the `action` attribute with its expected values pre-filled, or just type `action` and accept it from VS Code's native HTML attribute IntelliSense.

▶ allow
  Description: Grants an <iframe> permission to use browser features like camera or fullscreen.
  Syntax:      <iframe allow="fullscreen" src="video.html"></iframe>
  VSCode/Emmet: Inside an opening tag, start typing `allow` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ allowfullscreen
  Description: Lets an embedded <iframe> request fullscreen mode.
  Syntax:      <iframe allowfullscreen src="player.html"></iframe>
  VSCode/Emmet: Inside an opening tag, start typing `allowfullscreen` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ alt
  Description: Alternative text describing an image, for accessibility and fallback.
  Syntax:      <img src="cat.jpg" alt="A sleeping cat">
  VSCode/Emmet: Inside an opening tag, start typing `alt` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ aria-checked
  Description: Indicates the checked state of a checkbox/radio/switch-like widget.
  Syntax:      <div role="checkbox" aria-checked="true"></div>
  VSCode/Emmet: Inside an opening tag, start typing `aria-checked` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ aria-current
  Description: Marks the current item within a set, e.g. the active page in navigation.
  Syntax:      <a href="/" aria-current="page">Home</a>
  VSCode/Emmet: Inside an opening tag, start typing `aria-current` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ aria-describedby
  Description: Points to the id of an element that provides extra descriptive text.
  Syntax:      <input aria-describedby="hint">
  VSCode/Emmet: Inside an opening tag, start typing `aria-describedby` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ aria-expanded
  Description: Indicates whether a collapsible element (menu, accordion) is currently expanded.
  Syntax:      <button aria-expanded="false">Menu</button>
  VSCode/Emmet: Inside an opening tag, start typing `aria-expanded` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ aria-hidden
  Description: Hides decorative/duplicate content from assistive technology while keeping it visible.
  Syntax:      <i class="icon" aria-hidden="true"></i>
  VSCode/Emmet: Inside an opening tag, start typing `aria-hidden` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ aria-label
  Description: Provides an accessible name when visible text isn't sufficient.
  Syntax:      <button aria-label="Close">&times;</button>
  VSCode/Emmet: Inside an opening tag, start typing `aria-label` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ aria-live
  Description: Announces dynamic content changes to screen readers without moving focus.
  Syntax:      <div aria-live="polite">3 new messages</div>
  VSCode/Emmet: Inside an opening tag, start typing `aria-live` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ async
  Description: Runs an external script asynchronously without blocking parsing.
  Syntax:      <script async src="a.js"></script>
  VSCode/Emmet: Inside an opening tag, start typing `async` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ autocomplete
  Description: Hints whether the browser may auto-fill a field's value.
  Syntax:      <input autocomplete="off">
  VSCode/Emmet: Inside an opening tag, start typing `autocomplete` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ autofocus
  Description: Automatically focuses the element when the page loads.
  Syntax:      <input autofocus>
  VSCode/Emmet: Inside an opening tag, start typing `autofocus` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ autoplay
  Description: Starts audio/video playback automatically as soon as possible.
  Syntax:      <video autoplay muted src="loop.mp4"></video>
  VSCode/Emmet: Inside an opening tag, start typing `autoplay` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ charset
  Description: Declares the character encoding of the document.
  Syntax:      <meta charset="UTF-8">
  VSCode/Emmet: Inside an opening tag, type the Emmet snippet `meta:utf` and press Tab/Enter to insert the `charset` attribute with its expected values pre-filled, or just type `charset` and accept it from VS Code's native HTML attribute IntelliSense.

▶ checked
  Description: Marks a checkbox/radio as selected by default.
  Syntax:      <input type="checkbox" checked>
  VSCode/Emmet: Inside an opening tag, start typing `checked` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ class
  Description: Assigns one or more class names for CSS/JS targeting.
  Syntax:      <div class="card active"></div>
  VSCode/Emmet: Inside an opening tag, start typing `class` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ colspan
  Description: The number of columns a table cell should span.
  Syntax:      <td colspan="2">Merged</td>
  VSCode/Emmet: Inside an opening tag, start typing `colspan` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ content
  Description: The value paired with a <meta> element's name/http-equiv.
  Syntax:      <meta name="description" content="...">
  VSCode/Emmet: Inside an opening tag, type the Emmet snippet `meta:desc` and press Tab/Enter to insert the `content` attribute with its expected values pre-filled, or just type `content` and accept it from VS Code's native HTML attribute IntelliSense.

▶ contenteditable
  Description: Makes the element's content directly editable by the user.
  Syntax:      <div contenteditable="true">Edit me</div>
  VSCode/Emmet: Inside an opening tag, start typing `contenteditable` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ controls
  Description: Shows the browser's built-in playback controls for audio/video.
  Syntax:      <video controls src="movie.mp4"></video>
  VSCode/Emmet: Inside an opening tag, start typing `controls` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ crossorigin
  Description: Configures CORS requests for the element's fetched resource.
  Syntax:      <script crossorigin src="cdn.js"></script>
  VSCode/Emmet: Inside an opening tag, start typing `crossorigin` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ data-*
  Description: Stores custom private data on an element, readable via JS dataset.
  Syntax:      <li data-id="42">Item</li>
  VSCode/Emmet: Inside an opening tag, start typing `data-*` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ datetime
  Description: Machine-readable date/time value for <time>, <ins>, or <del>.
  Syntax:      <time datetime="2026-07-11">July 11</time>
  VSCode/Emmet: Inside an opening tag, start typing `datetime` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ decoding
  Description: Hints whether an image should decode synchronously or asynchronously.
  Syntax:      <img decoding="async" src="photo.jpg">
  VSCode/Emmet: Inside an opening tag, start typing `decoding` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ defer
  Description: Runs a script only after the document has been parsed.
  Syntax:      <script defer src="a.js"></script>
  VSCode/Emmet: Inside an opening tag, start typing `defer` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ dir
  Description: Sets text direction: ltr, rtl, or auto.
  Syntax:      <p dir="rtl">مرحبا</p>
  VSCode/Emmet: Inside an opening tag, start typing `dir` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ disabled
  Description: Disables a form control so it can't be interacted with.
  Syntax:      <button disabled>Can't click</button>
  VSCode/Emmet: Inside an opening tag, start typing `disabled` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ download
  Description: Marks a hyperlink to be downloaded rather than navigated to.
  Syntax:      <a href="file.pdf" download>Download</a>
  VSCode/Emmet: Inside an opening tag, start typing `download` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ draggable
  Description: Indicates whether the element can be dragged.
  Syntax:      <img draggable="true" src="x.png">
  VSCode/Emmet: Inside an opening tag, start typing `draggable` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ enctype
  Description: The encoding type used when submitting a form via POST.
  Syntax:      <form enctype="multipart/form-data">
  VSCode/Emmet: Inside an opening tag, start typing `enctype` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ enterkeyhint
  Description: Hints what label/icon to show on a virtual keyboard's Enter key.
  Syntax:      <input enterkeyhint="send">
  VSCode/Emmet: Inside an opening tag, start typing `enterkeyhint` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ fetchpriority
  Description: Hints the relative loading priority of an image/script/link.
  Syntax:      <img fetchpriority="high" src="hero.jpg">
  VSCode/Emmet: Inside an opening tag, start typing `fetchpriority` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ for
  Description: Associates a <label> with a form control by id.
  Syntax:      <label for="email">Email</label>
  VSCode/Emmet: Inside an opening tag, start typing `for` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ formaction
  Description: Overrides a submit button's form action URL for that click only.
  Syntax:      <button formaction="/preview">Preview</button>
  VSCode/Emmet: Inside an opening tag, start typing `formaction` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ height
  Description: Sets the intrinsic height for elements like img/canvas/video.
  Syntax:      <img src="x.jpg" height="150">
  VSCode/Emmet: Inside an opening tag, start typing `height` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ hidden
  Description: Hides the element from rendering while keeping it in the DOM.
  Syntax:      <div hidden>Not shown</div>
  VSCode/Emmet: Inside an opening tag, start typing `hidden` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ href
  Description: The URL a hyperlink points to.
  Syntax:      <a href="https://example.com">Link</a>
  VSCode/Emmet: Inside an opening tag, type the Emmet snippet `a[href]` and press Tab/Enter to insert the `href` attribute with its expected values pre-filled, or just type `href` and accept it from VS Code's native HTML attribute IntelliSense.

▶ id
  Description: Assigns a unique identifier to an element.
  Syntax:      <div id="main"></div>
  VSCode/Emmet: Inside an opening tag, start typing `id` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ inert
  Description: Makes an element (and its subtree) non-interactive and unfocusable.
  Syntax:      <div inert>Background content</div>
  VSCode/Emmet: Inside an opening tag, start typing `inert` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ inputmode
  Description: Hints which virtual keyboard to show for a text input (e.g. numeric, email).
  Syntax:      <input inputmode="numeric">
  VSCode/Emmet: Inside an opening tag, start typing `inputmode` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ integrity
  Description: Provides a Subresource Integrity hash to verify a fetched resource.
  Syntax:      <script integrity="sha384-..." src="cdn.js"></script>
  VSCode/Emmet: Inside an opening tag, start typing `integrity` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ lang
  Description: Declares the language of the element's content.
  Syntax:      <html lang="en">
  VSCode/Emmet: Inside an opening tag, start typing `lang` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ list
  Description: Associates an <input> with a <datalist> of suggested values.
  Syntax:      <input list="browsers">
  VSCode/Emmet: Inside an opening tag, start typing `list` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ loading
  Description: Controls whether an image/iframe loads lazily or eagerly.
  Syntax:      <img src="x.jpg" loading="lazy">
  VSCode/Emmet: Inside an opening tag, start typing `loading` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ loop
  Description: Restarts audio/video playback from the start when it finishes.
  Syntax:      <audio loop src="song.mp3"></audio>
  VSCode/Emmet: Inside an opening tag, start typing `loop` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ max
  Description: The maximum allowed value for a numeric/date/range input.
  Syntax:      <input type="number" max="100">
  VSCode/Emmet: Inside an opening tag, start typing `max` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ maxlength
  Description: The maximum number of characters allowed in an input/textarea.
  Syntax:      <input maxlength="20">
  VSCode/Emmet: Inside an opening tag, start typing `maxlength` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ method
  Description: The HTTP method (GET/POST) a form uses to submit.
  Syntax:      <form method="post">
  VSCode/Emmet: Inside an opening tag, type the Emmet snippet `form:post` and press Tab/Enter to insert the `method` attribute with its expected values pre-filled, or just type `method` and accept it from VS Code's native HTML attribute IntelliSense.

▶ min
  Description: The minimum allowed value for a numeric/date/range input.
  Syntax:      <input type="number" min="0">
  VSCode/Emmet: Inside an opening tag, start typing `min` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ multiple
  Description: Allows multiple values in a file/email input or select.
  Syntax:      <input type="file" multiple>
  VSCode/Emmet: Inside an opening tag, start typing `multiple` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ muted
  Description: Silences audio/video by default on page load.
  Syntax:      <video muted autoplay src="bg.mp4"></video>
  VSCode/Emmet: Inside an opening tag, start typing `muted` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ name
  Description: Identifies form controls when submitted, or names a frame/window.
  Syntax:      <input type="text" name="username">
  VSCode/Emmet: Inside an opening tag, start typing `name` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ novalidate
  Description: Skips built-in form validation on submit.
  Syntax:      <form novalidate>
  VSCode/Emmet: Inside an opening tag, start typing `novalidate` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ open
  Description: Indicates a <details>/<dialog> is currently visible/active.
  Syntax:      <details open>...</details>
  VSCode/Emmet: Inside an opening tag, start typing `open` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ pattern
  Description: A regular expression the input's value must match to be valid.
  Syntax:      <input pattern="[0-9]{5}">
  VSCode/Emmet: Inside an opening tag, start typing `pattern` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ placeholder
  Description: Shows hint text in an empty input/textarea.
  Syntax:      <input placeholder="Enter your name">
  VSCode/Emmet: Inside an opening tag, type the Emmet snippet `input:placeholder` and press Tab/Enter to insert the `placeholder` attribute with its expected values pre-filled, or just type `placeholder` and accept it from VS Code's native HTML attribute IntelliSense.

▶ popover
  Description: Marks an element as a native popover, shown/hidden via the Popover API.
  Syntax:      <div popover id="tip">Hello</div>
  VSCode/Emmet: Inside an opening tag, start typing `popover` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ poster
  Description: An image shown before a video starts playing.
  Syntax:      <video poster="thumb.jpg" src="movie.mp4"></video>
  VSCode/Emmet: Inside an opening tag, start typing `poster` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ preload
  Description: Hints how much of a media file to preload: none, metadata, or auto.
  Syntax:      <video preload="metadata" src="movie.mp4"></video>
  VSCode/Emmet: Inside an opening tag, start typing `preload` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ readonly
  Description: Makes an input/textarea non-editable but still submittable.
  Syntax:      <input readonly value="Fixed">
  VSCode/Emmet: Inside an opening tag, start typing `readonly` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ referrerpolicy
  Description: Controls how much referrer information is sent when fetching a resource.
  Syntax:      <img referrerpolicy="no-referrer" src="x.jpg">
  VSCode/Emmet: Inside an opening tag, start typing `referrerpolicy` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ rel
  Description: Specifies the relationship between the current document and the linked resource.
  Syntax:      <link rel="stylesheet" href="style.css">
  VSCode/Emmet: Inside an opening tag, start typing `rel` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ required
  Description: Marks a form field as mandatory before submission.
  Syntax:      <input required>
  VSCode/Emmet: Inside an opening tag, start typing `required` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ reversed
  Description: Displays an ordered list in descending order.
  Syntax:      <ol reversed><li>Third</li><li>Second</li><li>First</li></ol>
  VSCode/Emmet: Inside an opening tag, start typing `reversed` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ role
  Description: Defines an explicit ARIA role for assistive technologies.
  Syntax:      <div role="button">Click</div>
  VSCode/Emmet: Inside an opening tag, start typing `role` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ rowspan
  Description: The number of rows a table cell should span.
  Syntax:      <td rowspan="2">Tall cell</td>
  VSCode/Emmet: Inside an opening tag, start typing `rowspan` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ sandbox
  Description: Restricts what an embedded <iframe> is allowed to do.
  Syntax:      <iframe sandbox="allow-scripts" src="widget.html"></iframe>
  VSCode/Emmet: Inside an opening tag, start typing `sandbox` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ scope
  Description: On a <th>, defines whether it headers a row, column, or group of cells.
  Syntax:      <th scope="col">Name</th>
  VSCode/Emmet: Inside an opening tag, start typing `scope` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ sizes
  Description: Describes the display size of an image for use with srcset.
  Syntax:      <img sizes="(max-width:600px) 480px" srcset="...">
  VSCode/Emmet: Inside an opening tag, start typing `sizes` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ spellcheck
  Description: Indicates whether spell-checking is enabled for the element.
  Syntax:      <textarea spellcheck="false"></textarea>
  VSCode/Emmet: Inside an opening tag, start typing `spellcheck` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ src
  Description: The URL of embeddable content such as an image, script, or media file.
  Syntax:      <img src="photo.jpg">
  VSCode/Emmet: Inside an opening tag, type the Emmet snippet `img[src]` and press Tab/Enter to insert the `src` attribute with its expected values pre-filled, or just type `src` and accept it from VS Code's native HTML attribute IntelliSense.

▶ srcset
  Description: Lists responsive image candidates for the browser to choose from.
  Syntax:      <img srcset="s.jpg 480w, l.jpg 800w" src="l.jpg">
  VSCode/Emmet: Inside an opening tag, type the Emmet snippet `img:srcset` and press Tab/Enter to insert the `srcset` attribute with its expected values pre-filled, or just type `srcset` and accept it from VS Code's native HTML attribute IntelliSense.

▶ start
  Description: Sets the starting number of an ordered list.
  Syntax:      <ol start="5"><li>Five</li><li>Six</li></ol>
  VSCode/Emmet: Inside an opening tag, start typing `start` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ step
  Description: The interval of legal values for a numeric/range input.
  Syntax:      <input type="number" step="5">
  VSCode/Emmet: Inside an opening tag, start typing `step` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ style
  Description: Applies inline CSS styles directly to an element.
  Syntax:      <p style="color:red">Text</p>
  VSCode/Emmet: Inside an opening tag, start typing `style` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ tabindex
  Description: Controls whether/how an element participates in keyboard tab order.
  Syntax:      <div tabindex="0">Focusable</div>
  VSCode/Emmet: Inside an opening tag, start typing `tabindex` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ target
  Description: Where to open the linked document (e.g. _blank).
  Syntax:      <a href="url" target="_blank">Link</a>
  VSCode/Emmet: Inside an opening tag, start typing `target` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ title
  Description: Provides advisory text shown as a tooltip on hover.
  Syntax:      <abbr title="World Health Organization">WHO</abbr>
  VSCode/Emmet: Inside an opening tag, start typing `title` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ type
  Description: Specifies the type of an <input>, <button>, <script>, or <link>.
  Syntax:      <input type="email">
  VSCode/Emmet: Inside an opening tag, start typing `type` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ usemap
  Description: Associates an <img> with a <map> of clickable areas.
  Syntax:      <img src="map.png" usemap="#worldmap">
  VSCode/Emmet: Inside an opening tag, start typing `usemap` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ value
  Description: Sets the default/current value of a form control.
  Syntax:      <input type="text" value="Hello">
  VSCode/Emmet: Inside an opening tag, start typing `value` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ width
  Description: Sets the intrinsic width for elements like img/canvas/video.
  Syntax:      <img src="x.jpg" width="200">
  VSCode/Emmet: Inside an opening tag, start typing `width` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.

▶ wrap
  Description: Controls whether a <textarea>'s text wraps when submitted: soft or hard.
  Syntax:      <textarea wrap="hard"></textarea>
  VSCode/Emmet: Inside an opening tag, start typing `wrap` -- VS Code's built-in HTML IntelliSense will suggest it; press Enter/Tab to accept.


[Category: Elements] (116 entries)
--------------------------------------------------------------------------------
▶ `a`
  Description: A hyperlink used to link to other pages or websites.
  Syntax:      <a href=""></a>
  VSCode/Emmet: Type `a`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<a></a>`.

▶ `body`
  Description: Contains all the visible content (text, images, links) of the webpage.
  Syntax:      <body></body>
  VSCode/Emmet: Type `body`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<body></body>`.

▶ `br`
  Description: Forces a line break (like pressing Enter) without starting a new paragraph.
  Syntax:      <br>
  VSCode/Emmet: Type `br`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<br></br>`.

▶ `div`
  Description: A generic container used to group elements together for styling (CSS) or layout.
  Syntax:      <div></div>
  VSCode/Emmet: Type `div`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<div></div>`.

▶ `head`
  Description: Contains invisible meta-information like the page title and CSS links.
  Syntax:      <head></head>
  VSCode/Emmet: Type `head`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<head></head>`.

▶ `hr`
  Description: Draws a horizontal line across the page to visually separate sections.
  Syntax:      <hr>
  VSCode/Emmet: Type `hr`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<hr></hr>`.

▶ `html:5`
  Description: Creates the entire base structure required for any standard webpage.
  Syntax:      `html:5`
  VSCode/Emmet: Type `html:5`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<html:5></html:5>`.

▶ `html`
  Description: The root element that wraps all content on the page.
  Syntax:      <html></html>
  VSCode/Emmet: Type `html`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<html></html>`.

▶ `img`
  Description: Embeds an image into the page.
  Syntax:      <img src="" alt="">
  VSCode/Emmet: Type `img`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<img></img>`.

▶ `input`
  Description: Creates an interactive field (like a text box) for users to type into.
  Syntax:      <input type="">
  VSCode/Emmet: Type `input`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<input></input>`.

▶ `link`
  Description: Links your HTML file to an external CSS stylesheet file.
  Syntax:      <link rel="stylesheet" href="">
  VSCode/Emmet: Type `link`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<link></link>`.

▶ `meta`
  Description: Defines data about the document, like the character set or viewport sizing for mobile.
  Syntax:      <meta charset="UTF-8">
  VSCode/Emmet: Type `meta`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<meta></meta>`.

▶ `p`
  Description: A standard paragraph for blocks of text.
  Syntax:      <p></p>
  VSCode/Emmet: Type `p`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<p></p>`.

▶ `script:src`
  Description: Links your HTML file to an external JavaScript file.
  Syntax:      <script src=""></script>
  VSCode/Emmet: Type `script:src`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<script:src></script:src>`.

▶ `span`
  Description: An inline container used to style or color a small piece of text inside a paragraph.
  Syntax:      <span></span>
  VSCode/Emmet: Type `span`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<span></span>`.

▶ `style`
  Description: Allows you to write CSS code directly inside your HTML file.
  Syntax:      <style></style>
  VSCode/Emmet: Type `style`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<style></style>`.

▶ <abbr>
  Description: Marks an abbreviation or acronym; use the title attribute for the full form.
  Syntax:      <abbr title="HyperText Markup Language">HTML</abbr>
  VSCode/Emmet: Type `abbr`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<abbr></abbr>`.

▶ <address>
  Description: Marks up contact information for a person, people, or organization.
  Syntax:      <address>Contact us at info@example.com</address>
  VSCode/Emmet: Type `adr` (the Emmet alias) or the full tag name `address`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<address></address>`.

▶ <area>
  Description: Defines a clickable region inside an <img> that uses a <map>.
  Syntax:      <area shape="rect" coords="0,0,50,50" href="#">
  VSCode/Emmet: Type `area`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<area></area>`.

▶ <article>
  Description: Independent, self-contained content.
  Syntax:      <article>
  VSCode/Emmet: Type `art` (the Emmet alias) or the full tag name `article`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<article></article>`.

▶ <aside>
  Description: Sidebar / tangential content.
  Syntax:      <aside>
  VSCode/Emmet: Type `aside`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<aside></aside>`.

▶ <audio>
  Description: Embeds sound content with one or more sources.
  Syntax:      <audio controls src="song.mp3"></audio>
  VSCode/Emmet: Type `audio`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<audio></audio>`.

▶ <b>
  Description: Draws attention to text without implying extra importance (use CSS font-weight for styling).
  Syntax:      <b>Keyword</b>
  VSCode/Emmet: Type `b`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<b></b>`.

▶ <base>
  Description: Sets the base URL for all relative URLs in the document.
  Syntax:      <base href="https://example.com/">
  VSCode/Emmet: Type `base`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<base></base>`.

▶ <bdi>
  Description: Isolates text whose directionality is unknown from the surrounding text.
  Syntax:      <bdi>اسم المستخدم</bdi>
  VSCode/Emmet: Type `bdi`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<bdi></bdi>`.

▶ <bdo>
  Description: Overrides the current text direction.
  Syntax:      <bdo dir="rtl">Text</bdo>
  VSCode/Emmet: Type `bdo`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<bdo></bdo>`.

▶ <blockquote>
  Description: Marks an extended quotation, usually rendered as an indented block.
  Syntax:      <blockquote cite="url">Quoted text</blockquote>
  VSCode/Emmet: Type `bq` (the Emmet alias) or the full tag name `blockquote`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<blockquote></blockquote>`.

▶ <button>
  Description: An interactive control that performs an action when activated.
  Syntax:      <button type="submit">Send</button>
  VSCode/Emmet: Type `btn` (the Emmet alias) or the full tag name `button`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<button></button>`.

▶ <canvas>
  Description: A drawable region used with the Canvas API or WebGL for graphics/animation.
  Syntax:      <canvas id="c" width="200" height="100"></canvas>
  VSCode/Emmet: Type `canvas`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<canvas></canvas>`.

▶ <caption>
  Description: Specifies the caption/title of a <table>.
  Syntax:      <table><caption>Sales</caption>...</table>
  VSCode/Emmet: Type `cap` (the Emmet alias) or the full tag name `caption`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<caption></caption>`.

▶ <cite>
  Description: Marks the title of a referenced creative work.
  Syntax:      <cite>The Hobbit</cite>
  VSCode/Emmet: Type `cite`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<cite></cite>`.

▶ <code>
  Description: Marks a short fragment of computer code, rendered in monospace.
  Syntax:      <code>const x = 1;</code>
  VSCode/Emmet: Type `code`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<code></code>`.

▶ <col>
  Description: Defines properties for one or more columns in a <colgroup>.
  Syntax:      <colgroup><col span="2"></colgroup>
  VSCode/Emmet: Type `col`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<col></col>`.

▶ <colgroup>
  Description: Defines a group of columns within a table.
  Syntax:      <colgroup><col></colgroup>
  VSCode/Emmet: Type `colg` (the Emmet alias) or the full tag name `colgroup`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<colgroup></colgroup>`.

▶ <data>
  Description: Links content with a machine-readable value.
  Syntax:      <data value="21053">Model 21053</data>
  VSCode/Emmet: Type `data`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<data></data>`.

▶ <datalist>
  Description: Holds a set of <option> elements suggested for an <input>.
  Syntax:      <datalist id="colors"><option value="Red"></datalist>
  VSCode/Emmet: Type `datal` (the Emmet alias) or the full tag name `datalist`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<datalist></datalist>`.

▶ <dd>
  Description: Provides the description/value for a preceding <dt> term.
  Syntax:      <dl><dt>HTML</dt><dd>HyperText Markup Language</dd></dl>
  VSCode/Emmet: Type `dd`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<dd></dd>`.

▶ <del>
  Description: Marks text that has been deleted from the document.
  Syntax:      <del>old price</del>
  VSCode/Emmet: Type `del`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<del></del>`.

▶ <details>
  Description: Creates a disclosure widget that is toggled open/closed.
  Syntax:      <details><summary>More</summary>Hidden</details>
  VSCode/Emmet: Type `det` (the Emmet alias) or the full tag name `details`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<details></details>`.

▶ <details>+<summary>
  Description: Toggleable disclosure widget.
  Syntax:      <details>+<summary>
  VSCode/Emmet: Type `details>+<summary`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<details>+<summary></details>+<summary>`.

▶ <dfn>
  Description: Marks the term being defined within a definition sentence.
  Syntax:      <dfn>DOM</dfn> is the Document Object Model.
  VSCode/Emmet: Type `dfn`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<dfn></dfn>`.

▶ <dialog>
  Description: Represents a dialog box or other interactive component.
  Syntax:      <dialog open>Message</dialog>
  VSCode/Emmet: Type `dlg` (the Emmet alias) or the full tag name `dialog`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<dialog></dialog>`.

▶ <dl>
  Description: A description list of term/description pairs.
  Syntax:      <dl><dt>Term</dt><dd>Definition</dd></dl>
  VSCode/Emmet: Type `dl`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<dl></dl>`.

▶ <dt>
  Description: A term in a description list, followed by a <dd>.
  Syntax:      <dt>CSS</dt>
  VSCode/Emmet: Type `dt`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<dt></dt>`.

▶ <em>
  Description: Marks stressed emphasis; can be nested for stronger emphasis.
  Syntax:      <em>really</em> important
  VSCode/Emmet: Type `em`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<em></em>`.

▶ <embed>
  Description: Embeds external content provided by a plugin or browser feature.
  Syntax:      <embed src="file.pdf">
  VSCode/Emmet: Type `emb` (the Emmet alias) or the full tag name `embed`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<embed></embed>`.

▶ <fieldset>
  Description: Groups related form controls and labels together.
  Syntax:      <fieldset><legend>Contact</legend>...</fieldset>
  VSCode/Emmet: Type `fst` (the Emmet alias) or the full tag name `fieldset`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<fieldset></fieldset>`.

▶ <figure>+<figcaption>
  Description: Media with a caption.
  Syntax:      <figure>+<figcaption>
  VSCode/Emmet: Type `figure>+<figcaption`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<figure>+<figcaption></figure>+<figcaption>`.

▶ <footer>
  Description: Footer for its nearest sectioning ancestor: author, copyright, related links.
  Syntax:      <footer>&copy; 2026 Company</footer>
  VSCode/Emmet: Type `ftr` (the Emmet alias) or the full tag name `footer`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<footer></footer>`.

▶ <form>
  Description: A section containing interactive controls for submitting information.
  Syntax:      <form action="/submit" method="post">...</form>
  VSCode/Emmet: Type `form`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<form></form>`.

▶ <h1>
  Description: Top-level section heading.
  Syntax:      <h1>Page Title</h1>
  VSCode/Emmet: Type `h1`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<h1></h1>`.

▶ <h2>
  Description: Second-level section heading.
  Syntax:      <h2>Section Title</h2>
  VSCode/Emmet: Type `h2`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<h2></h2>`.

▶ <h3>
  Description: Third-level section heading.
  Syntax:      <h3>Subsection Title</h3>
  VSCode/Emmet: Type `h3`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<h3></h3>`.

▶ <h4>
  Description: Fourth-level section heading.
  Syntax:      <h4>Minor Heading</h4>
  VSCode/Emmet: Type `h4`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<h4></h4>`.

▶ <h5>
  Description: Fifth-level section heading.
  Syntax:      <h5>Small Heading</h5>
  VSCode/Emmet: Type `h5`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<h5></h5>`.

▶ <h6>
  Description: Sixth (lowest) level section heading.
  Syntax:      <h6>Smallest Heading</h6>
  VSCode/Emmet: Type `h6`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<h6></h6>`.

▶ <header>
  Description: Top section: logo, nav.
  Syntax:      <header>
  VSCode/Emmet: Type `hdr` (the Emmet alias) or the full tag name `header`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<header></header>`.

▶ <hgroup>
  Description: Groups a heading with related secondary content like a subtitle.
  Syntax:      <hgroup><h1>Title</h1><p>Subtitle</p></hgroup>
  VSCode/Emmet: Type `hgroup`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<hgroup></hgroup>`.

▶ <html>
  Description: Root element wrapping the entire document.
  Syntax:      <html lang="en">...</html>
  VSCode/Emmet: Type `html`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<html></html>`.

▶ <i>
  Description: Marks text in an alternate voice/mood, such as technical terms or foreign words.
  Syntax:      <i>lingua franca</i>
  VSCode/Emmet: Type `i`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<i></i>`.

▶ <iframe>
  Description: Embeds another HTML page as a nested browsing context.
  Syntax:      <iframe src="https://example.com"></iframe>
  VSCode/Emmet: Type `ifr` (the Emmet alias) or the full tag name `iframe`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<iframe></iframe>`.

▶ <ins>
  Description: Marks text that has been added to the document.
  Syntax:      <ins>new price</ins>
  VSCode/Emmet: Type `ins`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<ins></ins>`.

▶ <kbd>
  Description: Marks textual user input from a keyboard or similar device.
  Syntax:      Press <kbd>Ctrl</kbd>+<kbd>C</kbd>
  VSCode/Emmet: Type `kbd`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<kbd></kbd>`.

▶ <label>
  Description: A caption for an item in a user interface, associated with a form control.
  Syntax:      <label for="email">Email</label>
  VSCode/Emmet: Type `label`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<label></label>`.

▶ <legend>
  Description: A caption for the content of its parent <fieldset>.
  Syntax:      <legend>Personal Info</legend>
  VSCode/Emmet: Type `leg` (the Emmet alias) or the full tag name `legend`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<legend></legend>`.

▶ <li>
  Description: A single item within an <ol>, <ul>, or <menu> list.
  Syntax:      <ul>
  VSCode/Emmet: Type `li`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<li></li>`.

▶ <main>
  Description: Primary content of the page.
  Syntax:      <main>
  VSCode/Emmet: Type `mn` (the Emmet alias) or the full tag name `main`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<main></main>`.

▶ <map>
  Description: Defines an image map used together with <area> elements.
  Syntax:      <map name="map"><area ...></map>
  VSCode/Emmet: Type `map`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<map></map>`.

▶ <mark>
  Description: Highlights text for reference or notation purposes.
  Syntax:      <mark>highlighted</mark> text
  VSCode/Emmet: Type `mark`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<mark></mark>`.

▶ <math>
  Description: Top-level element for embedding MathML content.
  Syntax:      <math><mi>x</mi></math>
  VSCode/Emmet: Type `math`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<math></math>`.

▶ <menu>
  Description: Semantic alternative to <ul>, treated the same by browsers.
  Syntax:      <menu><li>Item</li></menu>
  VSCode/Emmet: Type `menu`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<menu></menu>`.

▶ <meter>
  Description: Represents a scalar value within a known range, like disk usage.
  Syntax:      <meter value="0.6" min="0" max="1"></meter>
  VSCode/Emmet: Type `meter`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<meter></meter>`.

▶ <nav>
  Description: Section containing primary navigation links.
  Syntax:      <nav><a href="/">Home</a></nav>
  VSCode/Emmet: Type `nav`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<nav></nav>`.

▶ <noscript>
  Description: Content shown when scripting is disabled or unsupported.
  Syntax:      <noscript>Please enable JavaScript.</noscript>
  VSCode/Emmet: Type `noscript`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<noscript></noscript>`.

▶ <object>
  Description: Embeds an external resource treated as an image, nested browsing context, or plugin-handled content.
  Syntax:      <object data="file.pdf" type="application/pdf"></object>
  VSCode/Emmet: Type `obj` (the Emmet alias) or the full tag name `object`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<object></object>`.

▶ <ol>
  Description: An ordered (numbered) list.
  Syntax:      <ol><li>First</li><li>Second</li></ol>
  VSCode/Emmet: Type `ol`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<ol></ol>`.

▶ <optgroup>
  Description: Groups related <option> elements within a <select>.
  Syntax:      <optgroup label="Fruits"><option>Apple</option></optgroup>
  VSCode/Emmet: Type `optg` (the Emmet alias) or the full tag name `optgroup`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<optgroup></optgroup>`.

▶ <option>
  Description: A single item within a <select>, <optgroup>, or <datalist>.
  Syntax:      <option value="1">One</option>
  VSCode/Emmet: Type `option`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<option></option>`.

▶ <output>
  Description: Container for injecting the result of a calculation or user action.
  Syntax:      <output name="result">42</output>
  VSCode/Emmet: Type `out` (the Emmet alias) or the full tag name `output`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<output></output>`.

▶ <picture>
  Description: Responsive image sources.
  Syntax:      <picture>
  VSCode/Emmet: Type `pic` (the Emmet alias) or the full tag name `picture`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<picture></picture>`.

▶ <pre>
  Description: Preformatted text rendered exactly as written, in a monospace font.
  Syntax:      <pre>  indented
  VSCode/Emmet: Type `pre`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<pre></pre>`.

▶ <progress>
  Description: Displays completion progress of a task as a progress bar.
  Syntax:      <progress value="70" max="100"></progress>
  VSCode/Emmet: Type `prog` (the Emmet alias) or the full tag name `progress`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<progress></progress>`.

▶ <q>
  Description: A short inline quotation, rendered with quotation marks.
  Syntax:      <q>To be or not to be</q>
  VSCode/Emmet: Type `q`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<q></q>`.

▶ <rp>
  Description: Provides fallback parentheses for ruby annotations in unsupported browsers.
  Syntax:      <ruby>漢<rp>(</rp><rt>Kan</rt><rp>)</rp></ruby>
  VSCode/Emmet: Type `rp`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<rp></rp>`.

▶ <rt>
  Description: The pronunciation/translation text of a ruby annotation.
  Syntax:      <rt>Kan</rt>
  VSCode/Emmet: Type `rt`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<rt></rt>`.

▶ <ruby>
  Description: Renders small annotations above/below base text, for East Asian typography.
  Syntax:      <ruby>漢<rt>Kan</rt></ruby>
  VSCode/Emmet: Type `ruby`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<ruby></ruby>`.

▶ <s>
  Description: Renders text with a strikethrough, indicating it is no longer relevant.
  Syntax:      <s>$50</s> $30
  VSCode/Emmet: Type `s`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<s></s>`.

▶ <samp>
  Description: Encloses sample output from a computer program.
  Syntax:      <samp>File not found</samp>
  VSCode/Emmet: Type `samp`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<samp></samp>`.

▶ <search>
  Description: Groups a set of form controls or content used for searching/filtering.
  Syntax:      <search><input type="search"></search>
  VSCode/Emmet: Type `search`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<search></search>`.

▶ <section>
  Description: Standalone thematic grouping.
  Syntax:      <section>
  VSCode/Emmet: Type `sect` (the Emmet alias) or the full tag name `section`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<section></section>`.

▶ <select>
  Description: A control offering a menu of options to choose from.
  Syntax:      <select><option>One</option></select>
  VSCode/Emmet: Type `select`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<select></select>`.

▶ <slot>
  Description: A placeholder inside a web component that can be filled with markup.
  Syntax:      <slot name="header"></slot>
  VSCode/Emmet: Type `slot`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<slot></slot>`.

▶ <small>
  Description: Represents side-comments and small print, rendered one size smaller.
  Syntax:      <small>Terms apply</small>
  VSCode/Emmet: Type `small`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<small></small>`.

▶ <source>
  Description: Specifies alternative media resources for <picture>, <audio>, or <video>.
  Syntax:      <source srcset="img.webp" type="image/webp">
  VSCode/Emmet: Type `source`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<source></source>`.

▶ <strong>
  Description: Indicates strong importance; typically rendered bold.
  Syntax:      <strong>Warning:</strong> Do not proceed
  VSCode/Emmet: Type `str` (the Emmet alias) or the full tag name `strong`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<strong></strong>`.

▶ <sub>
  Description: Renders text as subscript.
  Syntax:      H<sub>2</sub>O
  VSCode/Emmet: Type `sub`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<sub></sub>`.

▶ <summary>
  Description: Provides the summary/label for a <details> disclosure box.
  Syntax:      <summary>Click to expand</summary>
  VSCode/Emmet: Type `sum` (the Emmet alias) or the full tag name `summary`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<summary></summary>`.

▶ <sup>
  Description: Renders text as superscript.
  Syntax:      x<sup>2</sup>
  VSCode/Emmet: Type `sup`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<sup></sup>`.

▶ <svg>
  Description: Defines a coordinate system/viewport for embedding Scalable Vector Graphics.
  Syntax:      <svg viewBox="0 0 100 100"><circle cx="50" cy="50" r="40"/></svg>
  VSCode/Emmet: Type `svg`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<svg></svg>`.

▶ <table>
  Description: Represents tabular data as rows and columns of cells.
  Syntax:      <table><tr><td>1</td></tr></table>
  VSCode/Emmet: Type `table`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<table></table>`.

▶ <tbody>
  Description: Groups the main body rows of a table's data.
  Syntax:      <tbody><tr><td>Row</td></tr></tbody>
  VSCode/Emmet: Type `tbody`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<tbody></tbody>`.

▶ <td>
  Description: A single data cell within a table row.
  Syntax:      <td>Cell value</td>
  VSCode/Emmet: Type `td`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<td></td>`.

▶ <template>
  Description: Holds HTML that isn't rendered immediately but can be cloned via JS.
  Syntax:      <template id="row"><tr><td></td></tr></template>
  VSCode/Emmet: Type `tem` (the Emmet alias) or the full tag name `template`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<template></template>`.

▶ <textarea>
  Description: A multi-line plain-text editing control.
  Syntax:      <textarea rows="4" cols="40"></textarea>
  VSCode/Emmet: Type `tarea` (the Emmet alias) or the full tag name `textarea`, then press Tab (with the VS Code setting "emmet.triggerExpansionOnTab": true) -- or pick the Emmet suggestion from the completion list and press Enter -- to expand it to `<textarea></textarea>`.

▶ <tfoot>
  Description: Groups the footer rows summarizing a table's columns.
  Syntax:      <tfoot><tr><td>Total</td></tr></tfoot>
  VSCode/Emmet: Type `tfoot`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<tfoot></tfoot>`.

▶ <th>
  Description: A header cell within a table row.
  Syntax:      <th scope="col">Name</th>
  VSCode/Emmet: Type `th`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<th></th>`.

▶ <thead>
  Description: Groups the header rows describing a table's columns.
  Syntax:      <thead><tr><th>Name</th></tr></thead>
  VSCode/Emmet: Type `thead`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<thead></thead>`.

▶ <time>
  Description: Represents a specific date/time, with an optional machine-readable datetime.
  Syntax:      <time datetime="2026-07-11">July 11</time>
  VSCode/Emmet: Type `time`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<time></time>`.

▶ <title>
  Description: Sets the document title shown in the browser tab.
  Syntax:      <title>My Page</title>
  VSCode/Emmet: Type `title`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<title></title>`.

▶ <tr>
  Description: Defines a row of cells within a table.
  Syntax:      <tr><td>A</td><td>B</td></tr>
  VSCode/Emmet: Type `tr`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<tr></tr>`.

▶ <track>
  Description: Specifies timed text tracks (e.g. subtitles) for <audio>/<video>.
  Syntax:      <track src="subs.vtt" kind="subtitles">
  VSCode/Emmet: Type `track`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<track></track>`.

▶ <u>
  Description: Renders an unarticulated, non-textual annotation, by default an underline.
  Syntax:      <u>Misspelled</u>
  VSCode/Emmet: Type `u`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<u></u>`.

▶ <ul>
  Description: An unordered (bulleted) list.
  Syntax:      <ul><li>Item one</li><li>Item two</li></ul>
  VSCode/Emmet: Type `ul`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<ul></ul>`.

▶ <var>
  Description: Represents the name of a variable in a mathematical or programming context.
  Syntax:      <var>x</var> = 5
  VSCode/Emmet: Type `var`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<var></var>`.

▶ <video>
  Description: Embeds a video player, with optional multiple <source> elements.
  Syntax:      <video controls src="movie.mp4"></video>
  VSCode/Emmet: Type `video`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<video></video>`.

▶ <wbr>
  Description: Marks an optional line-break opportunity within text.
  Syntax:      supercalifragilistic<wbr>expialidocious
  VSCode/Emmet: Type `wbr`, then press Tab (with "emmet.triggerExpansionOnTab": true enabled) or select the Emmet suggestion from the IntelliSense list and press Enter/Tab to expand it to `<wbr></wbr>`.


[Category: Emmet] (22 entries)
--------------------------------------------------------------------------------
▶ !
  Description: Full HTML5 boilerplate (shorthand for html:5).
  Syntax:      <!DOCTYPE html>
  VSCode/Emmet: Type `!` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ art
  Description: Alias that expands to <article>.
  Syntax:      <article></article>
  VSCode/Emmet: Type `art` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ bq
  Description: Alias that expands to <blockquote>.
  Syntax:      <blockquote></blockquote>
  VSCode/Emmet: Type `bq` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ bq.quote>p*2
  Description: A blockquote with a class and two nested paragraphs.
  Syntax:      <blockquote class="quote">
  VSCode/Emmet: Type `bq.quote>p*2` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ btn
  Description: Alias that expands to <button>.
  Syntax:      <button></button>
  VSCode/Emmet: Type `btn` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ cc:ie
  Description: Wraps content in an IE-only conditional comment.
  Syntax:      <!--[if IE]>content<![endif]-->
  VSCode/Emmet: Type `cc:ie` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ dl>dt+dd*3
  Description: A description list with 3 term/description pairs.
  Syntax:      <dl>
  VSCode/Emmet: Type `dl>dt+dd*3` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ fig
  Description: Alias that expands to <figure>.
  Syntax:      <figure></figure>
  VSCode/Emmet: Type `fig` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ form>input+input+button
  Description: Form fields chained with +.
  Syntax:      form>input+input+button
  VSCode/Emmet: Type `form>input+input+button` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ ftr
  Description: Alias that expands to <footer>.
  Syntax:      <footer></footer>
  VSCode/Emmet: Type `ftr` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ hdr
  Description: Alias that expands to <header>.
  Syntax:      <header></header>
  VSCode/Emmet: Type `hdr` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ html:5
  Description: Full HTML5 boilerplate.
  Syntax:      html:5
  VSCode/Emmet: Type `html:5` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ img:s
  Description: Responsive image with srcset, src, and alt.
  Syntax:      <img srcset="" src="" alt="">
  VSCode/Emmet: Type `img:s` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ inp
  Description: Shorthand for a named/id'd input: input[name=$ id=$].
  Syntax:      <input type="text" name="" id="">
  VSCode/Emmet: Type `inp` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ li{Item $}*5
  Description: Numbered list items.
  Syntax:      li{Item $}*5
  VSCode/Emmet: Type `li{Item $}*5` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ mn
  Description: Alias that expands to <main>.
  Syntax:      <main></main>
  VSCode/Emmet: Type `mn` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ nav>ul>li*3>a
  Description: Nav with 3 links.
  Syntax:      nav>ul>li*3>a
  VSCode/Emmet: Type `nav>ul>li*3>a` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ p{Hello World}
  Description: Element with inner text.
  Syntax:      p{Hello World}
  VSCode/Emmet: Type `p{Hello World}` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ p>lorem10
  Description: 10 words of Lorem Ipsum.
  Syntax:      p>lorem10
  VSCode/Emmet: Type `p>lorem10` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ sect
  Description: Alias that expands to <section>.
  Syntax:      <section></section>
  VSCode/Emmet: Type `sect` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ table>tr*3>td*4
  Description: Table with 3 rows, 4 columns.
  Syntax:      table>tr*3>td*4
  VSCode/Emmet: Type `table>tr*3>td*4` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ ul>li*5
  Description: 5 nested list items.
  Syntax:      ul>li*5
  VSCode/Emmet: Type `ul>li*5` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.


[Category: Emmet Examples] (6 entries)
--------------------------------------------------------------------------------
▶ `form>input+input+button`
  Description: Complete Emmet examples
  Syntax:      `form>input+input+button`
  VSCode/Emmet: Type `form>input+input+button` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `header.logo+nav>ul>li*4>a`
  Description: Complete Emmet examples
  Syntax:      `header.logo+nav>ul>li*4>a`
  VSCode/Emmet: Type `header.logo+nav>ul>li*4>a` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `header>(nav>ul>li*4>a)+button`
  Description: Complete Emmet examples
  Syntax:      `header>(nav>ul>li*4>a)+button`
  VSCode/Emmet: Type `header>(nav>ul>li*4>a)+button` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `nav>ul>li*3>a`
  Description: Complete Emmet examples
  Syntax:      `nav>ul>li*3>a`
  VSCode/Emmet: Type `nav>ul>li*3>a` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `select>option*5`
  Description: Complete Emmet examples
  Syntax:      `select>option*5`
  VSCode/Emmet: Type `select>option*5` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `table>tr*3>td*4`
  Description: Complete Emmet examples
  Syntax:      `table>tr*3>td*4`
  VSCode/Emmet: Type `table>tr*3>td*4` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.


[Category: Emmet Grouping] (3 entries)
--------------------------------------------------------------------------------
▶ `(div.container>header+main)+footer`
  Description: Emmet grouping operators
  Syntax:      `(div.container>header+main)+footer`
  VSCode/Emmet: Type `(div.container>header+main)+footer` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `(header>nav)+main+footer`
  Description: Emmet grouping operators
  Syntax:      `(header>nav)+main+footer`
  VSCode/Emmet: Type `(header>nav)+main+footer` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `(parent>child)+sibling`
  Description: Emmet grouping operators
  Syntax:      `(parent>child)+sibling`
  VSCode/Emmet: Type `(parent>child)+sibling` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.


[Category: Emmet ID/Class] (5 entries)
--------------------------------------------------------------------------------
▶ `.container`
  Description: Emmet ID and Class attributes
  Syntax:      <div class="container"></div>
  VSCode/Emmet: Type `.container` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `#header`
  Description: Emmet ID and Class attributes
  Syntax:      <div id="header"></div>
  VSCode/Emmet: Type `#header` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `div.wrapper.main`
  Description: Emmet ID and Class attributes
  Syntax:      <div class="wrapper main"></div>
  VSCode/Emmet: Type `div.wrapper.main` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `div#app`
  Description: Emmet ID and Class attributes
  Syntax:      <div id="app"></div>
  VSCode/Emmet: Type `div#app` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `header#top.nav`
  Description: Emmet ID and Class attributes
  Syntax:      <header id="top" class="nav"></header>
  VSCode/Emmet: Type `header#top.nav` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.


[Category: Emmet Multiplication] (6 entries)
--------------------------------------------------------------------------------
▶ `div.item*4`
  Description: Emmet multiplication and numbering
  Syntax:      `div.item*4`
  VSCode/Emmet: Type `div.item*4` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `li{Count $}*5`
  Description: Emmet multiplication and numbering
  Syntax:      `li{Count $}*5`
  VSCode/Emmet: Type `li{Count $}*5` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `li${Item $}*5`
  Description: Emmet multiplication and numbering
  Syntax:      `li${Item $}*5`
  VSCode/Emmet: Type `li${Item $}*5` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `p.class${$}*3`
  Description: Emmet multiplication and numbering
  Syntax:      `p.class${$}*3`
  VSCode/Emmet: Type `p.class${$}*3` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `ul>li*3`
  Description: Emmet multiplication and numbering
  Syntax:      `ul>li*3`
  VSCode/Emmet: Type `ul>li*3` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `ul>li*5`
  Description: Emmet multiplication and numbering
  Syntax:      `ul>li*5`
  VSCode/Emmet: Type `ul>li*5` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.


[Category: Emmet Operators] (7 entries)
--------------------------------------------------------------------------------
▶ `div+p`
  Description: Emmet child or sibling operator
  Syntax:      <div></div><p></p>
  VSCode/Emmet: Type `div+p` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `div>p`
  Description: Emmet child or sibling operator
  Syntax:      <div><p></p></div>
  VSCode/Emmet: Type `div>p` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `div>p>span`
  Description: Emmet child or sibling operator
  Syntax:      <div><p><span></span></p></div>
  VSCode/Emmet: Type `div>p>span` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `header+main+footer`
  Description: Emmet child or sibling operator
  Syntax:      `header+main+footer`
  VSCode/Emmet: Type `header+main+footer` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `parent>child`
  Description: Emmet child or sibling operator
  Syntax:      `parent>child`
  VSCode/Emmet: Type `parent>child` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `sibling+sibling`
  Description: Emmet child or sibling operator
  Syntax:      `sibling+sibling`
  VSCode/Emmet: Type `sibling+sibling` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `ul>li*5`
  Description: Emmet child or sibling operator
  Syntax:      `ul>li*5`
  VSCode/Emmet: Type `ul>li*5` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.


[Category: Emmet Text Content] (6 entries)
--------------------------------------------------------------------------------
▶ `a{Click here}`
  Description: Emmet text content generation
  Syntax:      <a href="">Click here</a>
  VSCode/Emmet: Type `a{Click here}` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `div{Click me}`
  Description: Emmet text content generation
  Syntax:      <div>Click me</div>
  VSCode/Emmet: Type `div{Click me}` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `p{Hello World}`
  Description: Emmet text content generation
  Syntax:      <p>Hello World</p>
  VSCode/Emmet: Type `p{Hello World}` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `p>lorem`
  Description: Emmet text content generation
  Syntax:      `p>lorem`
  VSCode/Emmet: Type `p>lorem` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `p>lorem10`
  Description: Emmet text content generation
  Syntax:      `p>lorem10`
  VSCode/Emmet: Type `p>lorem10` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ `p>lorem100`
  Description: Emmet text content generation
  Syntax:      `p>lorem100`
  VSCode/Emmet: Type `p>lorem100` in a markup file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.


[Category: Entities] (42 entries)
--------------------------------------------------------------------------------
▶ &check;
  Description: Indicating success or completion
  Syntax:      Check mark ✓
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&check;`); select it and press Enter to insert it.

▶ &copy;
  Description: Copyright symbol.
  Syntax:      &copy;
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&copy;`); select it and press Enter to insert it.

▶ &cross;
  Description: Indicating failure or rejection
  Syntax:      Cross mark ✗
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&cross;`); select it and press Enter to insert it.

▶ &frac12;
  Description: Fractions in text
  Syntax:      One half ½
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&frac12;`); select it and press Enter to insert it.

▶ &frac14;
  Description: Fractions in text
  Syntax:      One quarter ¼
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&frac14;`); select it and press Enter to insert it.

▶ &hearts;
  Description: Card game or favorite symbols
  Syntax:      Heart suit ♥
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&hearts;`); select it and press Enter to insert it.

▶ &infin;
  Description: Mathematical notation
  Syntax:      Infinity ∞
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&infin;`); select it and press Enter to insert it.

▶ &laquo;
  Description: European-style opening quote
  Syntax:      Left angle quote «
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&laquo;`); select it and press Enter to insert it.

▶ &mdash;
  Description: Long dash for breaks in thought
  Syntax:      Em dash —
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&mdash;`); select it and press Enter to insert it.

▶ &middot;
  Description: Separator in menus or lists
  Syntax:      Middle dot ·
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&middot;`); select it and press Enter to insert it.

▶ &nbsp;
  Description: Non-breaking space.
  Syntax:      &nbsp;
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&nbsp;`); select it and press Enter to insert it.

▶ &ndash;
  Description: Ranges, e.g. 2020–2024
  Syntax:      En dash –
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&ndash;`); select it and press Enter to insert it.

▶ &plusmn;
  Description: Tolerances and ranges
  Syntax:      Plus-minus ±
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&plusmn;`); select it and press Enter to insert it.

▶ &raquo;
  Description: European-style closing quote
  Syntax:      Right angle quote »
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&raquo;`); select it and press Enter to insert it.

▶ &rarr;
  Description: Right arrow.
  Syntax:      &rarr;
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&rarr;`); select it and press Enter to insert it.

▶ &spades;
  Description: Card game symbols
  Syntax:      Spade suit ♠
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&spades;`); select it and press Enter to insert it.

▶ &star;
  Description: Ratings and favorites
  Syntax:      Star ★
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&star;`); select it and press Enter to insert it.

▶ &sup2;
  Description: Squared units, e.g. m²
  Syntax:      Superscript two ²
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&sup2;`); select it and press Enter to insert it.

▶ &sup3;
  Description: Cubed units, e.g. m³
  Syntax:      Superscript three ³
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&sup3;`); select it and press Enter to insert it.

▶ `"`
  Description: In attributes
  Syntax:      `"`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `"`); select it and press Enter to insert it.

▶ `&`
  Description: In text
  Syntax:      `&`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&`); select it and press Enter to insert it.

▶ `&bull;`
  Description: List items
  Syntax:      `&bull;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&bull;`); select it and press Enter to insert it.

▶ `&copy;`
  Description: Copyright symbol
  Syntax:      `&copy;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&copy;`); select it and press Enter to insert it.

▶ `&darr;`
  Description: Scroll indicator
  Syntax:      `&darr;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&darr;`); select it and press Enter to insert it.

▶ `&deg;`
  Description: Temperature/angles
  Syntax:      `&deg;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&deg;`); select it and press Enter to insert it.

▶ `&divide;`
  Description: Mathematical
  Syntax:      `&divide;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&divide;`); select it and press Enter to insert it.

▶ `&dollar;`
  Description: Currency
  Syntax:      `&dollar;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&dollar;`); select it and press Enter to insert it.

▶ `&euro;`
  Description: Currency
  Syntax:      `&euro;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&euro;`); select it and press Enter to insert it.

▶ `&hellip;`
  Description: Omissions
  Syntax:      `&hellip;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&hellip;`); select it and press Enter to insert it.

▶ `&larr;`
  Description: Navigation
  Syntax:      `&larr;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&larr;`); select it and press Enter to insert it.

▶ `&nbsp;`
  Description: Prevent line break
  Syntax:      `&nbsp;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&nbsp;`); select it and press Enter to insert it.

▶ `&para;`
  Description: Document markers
  Syntax:      `&para;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&para;`); select it and press Enter to insert it.

▶ `&pound;`
  Description: Currency
  Syntax:      `&pound;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&pound;`); select it and press Enter to insert it.

▶ `&rarr;`
  Description: Navigation
  Syntax:      `&rarr;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&rarr;`); select it and press Enter to insert it.

▶ `&reg;`
  Description: Trademark
  Syntax:      `&reg;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&reg;`); select it and press Enter to insert it.

▶ `&sect;`
  Description: Legal/technical
  Syntax:      `&sect;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&sect;`); select it and press Enter to insert it.

▶ `&times;`
  Description: Close buttons
  Syntax:      `&times;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&times;`); select it and press Enter to insert it.

▶ `&trade;`
  Description: TM symbol
  Syntax:      `&trade;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&trade;`); select it and press Enter to insert it.

▶ `&uarr;`
  Description: Scroll indicator
  Syntax:      `&uarr;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&uarr;`); select it and press Enter to insert it.

▶ `&yen;`
  Description: Currency
  Syntax:      `&yen;`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `&yen;`); select it and press Enter to insert it.

▶ `<`
  Description: In code display
  Syntax:      `<`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `<`); select it and press Enter to insert it.

▶ `>`
  Description: In code display
  Syntax:      `>`
  VSCode/Emmet: Type `&` inside text content -- VS Code's HTML language suggestions will list matching entities (e.g. `>`); select it and press Enter to insert it.


[Category: Notepad++] (2 entries)
--------------------------------------------------------------------------------
▶ Ctrl+D
  Description: Duplicate current line.
  Syntax:      Ctrl+D
  VSCode/Emmet: This is a Notepad++-specific shortcut (`Ctrl+D`), so it has no direct VS Code equivalent binding, though VS Code offers similar functionality via its own Command Palette actions and default keybindings.

▶ Ctrl+Shift+C
  Description: Comment out selection.
  Syntax:      Ctrl+Shift+C
  VSCode/Emmet: This is a Notepad++-specific shortcut (`Ctrl+Shift+C`), so it has no direct VS Code equivalent binding, though VS Code offers similar functionality via its own Command Palette actions and default keybindings.


[Category: Plugins] (1 entries)
--------------------------------------------------------------------------------
▶ Install Emmet in Notepad++ (No Installer, No Admin)
  Description: A step-by-step way to get Emmet-style abbreviation expansion working in Notepad++ using only ZIP downloads -- no .exe installer and no administrator password required.
  Syntax:      notepadpp-portable/
  VSCode/Emmet: Not applicable -- VS Code ships Emmet built in, so none of this manual plugin installation is needed there.


[Category: VS Code] (3 entries)
--------------------------------------------------------------------------------
▶ ! + Tab
  Description: Emmet HTML5 boilerplate.
  Syntax:      ! + Tab
  VSCode/Emmet: Press `! + Tab` directly in VS Code -- Emmet HTML5 boilerplate.

▶ Alt+Shift+F
  Description: Format the whole document.
  Syntax:      Alt+Shift+F
  VSCode/Emmet: Press `Alt+Shift+F` directly in VS Code -- Format the whole document.

▶ Ctrl+Shift+'
  Description: Wrap selection with abbreviation.
  Syntax:      Ctrl+Shift+'
  VSCode/Emmet: Press `Ctrl+Shift+'` directly in VS Code -- Wrap selection with abbreviation.


================================================================================
2. CSS REFERENCE INDEX (196 Styles & Properties)
================================================================================

[Category: Animation] (23 entries)
--------------------------------------------------------------------------------
▶ @keyframes
  Description: Defines multi-step animations.
  Syntax:      @keyframes slideIn {
  VSCode/Emmet: Start typing `@keyframes` in a CSS rule -- VS Code's native CSS IntelliSense will suggest the property; press Enter/Tab to accept it (Ctrl+Space to force the suggestion list).

▶ animation-delay
  Description: Sets a wait time before an animation starts.
  Syntax:      animation-delay: 0.5s;
  VSCode/Emmet: Start typing `animation-delay` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ animation-direction
  Description: Sets whether an animation plays forward, backward, or alternates.
  Syntax:      animation-direction: alternate;
  VSCode/Emmet: Start typing `animation-direction` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ animation-duration
  Description: Sets how long one animation cycle takes.
  Syntax:      animation-duration: 2s;
  VSCode/Emmet: Start typing `animation-duration` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ animation-fill-mode
  Description: Controls the styles applied before/after an animation runs.
  Syntax:      animation-fill-mode: forwards;
  VSCode/Emmet: Start typing `animation-fill-mode` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ animation-iteration-count
  Description: Sets how many times an animation repeats (or infinite).
  Syntax:      animation-iteration-count: infinite;
  VSCode/Emmet: Start typing `animation-iteration-count` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ animation-play-state
  Description: Pauses or resumes a running animation.
  Syntax:      animation-play-state: paused;
  VSCode/Emmet: Start typing `animation-play-state` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ animation-timing-function
  Description: Sets the animation's acceleration curve (ease, linear, cubic-bezier...).
  Syntax:      animation-timing-function: ease-in-out;
  VSCode/Emmet: Start typing `animation-timing-function` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ backface-visibility
  Description: Hides or shows the back side of elements when rotated away from the viewer in 3D space.
  Syntax:      backface-visibility: visible | hidden;
  VSCode/Emmet: Type `backface-visibility` to view autocomplete proposals; select `hidden`.

▶ matrix()
  Description: Applies a 2D transformation directly via a 6-value transformation matrix.
  Syntax:      transform: matrix(1, 0, 0, 1, 20, 10);
  VSCode/Emmet: Inside a property value, start typing `matrix()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.

▶ perspective
  Description: Defines the depth perspective scale of a parent container, determining the 3D depth of transformed children.
  Syntax:      perspective: none | length;
  VSCode/Emmet: Type `perspective` in a CSS block; VS Code will autocomplete and prompt you to input a pixel size.

▶ perspective()
  Description: Sets the distance from the viewer, affecting how 3D transforms appear.
  Syntax:      transform: perspective(500px) rotateY(20deg);
  VSCode/Emmet: Inside a property value, start typing `perspective()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.

▶ rotate3d()
  Description: Rotates an element in 3D space around an arbitrary axis.
  Syntax:      transform: rotate3d(1, 1, 0, 45deg);
  VSCode/Emmet: Inside a property value, start typing `rotate3d()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.

▶ scale3d()
  Description: Scales an element independently on the x, y, and z axes.
  Syntax:      transform: scale3d(1.2, 1.2, 1);
  VSCode/Emmet: Inside a property value, start typing `scale3d()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.

▶ skew()
  Description: Skews (shears) an element along the X and/or Y axis.
  Syntax:      transform: skew(10deg, 2deg);
  VSCode/Emmet: Inside a property value, start typing `skew()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.

▶ transform
  Description: Scale, rotate, translate elements.
  Syntax:      transform: scale(1.05) translateY(-4px);
  VSCode/Emmet: Type the Emmet abbreviation `trf` in a CSS rule and press Tab/Enter to expand it to `transform: ;` with the cursor ready for a value, or just start typing `transform` and accept VS Code's native CSS IntelliSense suggestion.

▶ transform-style: preserve-3d
  Description: Ensures child elements of a 3D container reside in individual 3D planes instead of being flattened.
  Syntax:      transform-style: flat | preserve-3d;
  VSCode/Emmet: Type `transform-style` inside a rule; VS Code will suggest `preserve-3d` and `flat` in its list.

▶ transition
  Description: Smooth property changes.
  Syntax:      transition: transform 0.3s ease;
  VSCode/Emmet: Type the Emmet abbreviation `trs` in a CSS rule and press Tab/Enter to expand it to `transition: ;` with the cursor ready for a value, or just start typing `transition` and accept VS Code's native CSS IntelliSense suggestion.

▶ transition-delay
  Description: Sets a wait time before a transition starts.
  Syntax:      transition-delay: 0.2s;
  VSCode/Emmet: Start typing `transition-delay` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ transition-property
  Description: Sets which CSS property(ies) a transition applies to.
  Syntax:      transition-property: transform, opacity;
  VSCode/Emmet: Start typing `transition-property` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ transition-timing-function: cubic-bezier
  Description: Specifies custom physics-based ease acceleration curves using cubic Bezier equations.
  Syntax:      transition-timing-function: cubic-bezier(x1, y1, x2, y2);
  VSCode/Emmet: Type `transition-timing-function` in CSS; VS Code suggests values like `cubic-bezier()` where you can fill the parameters.

▶ translate3d()
  Description: Moves an element in 3D space; often GPU-accelerated for smoother animation.
  Syntax:      transform: translate3d(0, -10px, 0);
  VSCode/Emmet: Inside a property value, start typing `translate3d()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.

▶ will-change
  Description: Hints to the browser which property will change, to optimize rendering ahead of time.
  Syntax:      will-change: transform, opacity;
  VSCode/Emmet: Start typing `will-change` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.


[Category: Box Model] (11 entries)
--------------------------------------------------------------------------------
▶ all
  Description: Resets every property on an element to a given value (initial/inherit/unset).
  Syntax:      all: unset;
  VSCode/Emmet: Start typing `all` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ box-sizing
  Description: Determines whether padding/border are included in width/height.
  Syntax:      box-sizing: border-box;
  VSCode/Emmet: Type the Emmet abbreviation `bxsz` in a CSS rule and press Tab/Enter to expand it to `box-sizing: ;` with the cursor ready for a value, or just start typing `box-sizing` and accept VS Code's native CSS IntelliSense suggestion.

▶ height
  Description: Sets the content height of an element.
  Syntax:      height: 200px;
  VSCode/Emmet: Type the Emmet abbreviation `h` in a CSS rule and press Tab/Enter to expand it to `height: ;` with the cursor ready for a value, or just start typing `height` and accept VS Code's native CSS IntelliSense suggestion.

▶ margin
  Description: Sets space outside an element's border on all sides.
  Syntax:      margin: 16px;
  VSCode/Emmet: Type the Emmet abbreviation `m` in a CSS rule and press Tab/Enter to expand it to `margin: ;` with the cursor ready for a value, or just start typing `margin` and accept VS Code's native CSS IntelliSense suggestion.

▶ max-height
  Description: Sets the maximum allowed height.
  Syntax:      max-height: 400px;
  VSCode/Emmet: Type the Emmet abbreviation `mah` in a CSS rule and press Tab/Enter to expand it to `max-height: ;` with the cursor ready for a value, or just start typing `max-height` and accept VS Code's native CSS IntelliSense suggestion.

▶ max-width
  Description: Sets the maximum allowed width.
  Syntax:      max-width: 100%;
  VSCode/Emmet: Type the Emmet abbreviation `maw` in a CSS rule and press Tab/Enter to expand it to `max-width: ;` with the cursor ready for a value, or just start typing `max-width` and accept VS Code's native CSS IntelliSense suggestion.

▶ min-height
  Description: Sets the minimum allowed height.
  Syntax:      min-height: 100vh;
  VSCode/Emmet: Type the Emmet abbreviation `mih` in a CSS rule and press Tab/Enter to expand it to `min-height: ;` with the cursor ready for a value, or just start typing `min-height` and accept VS Code's native CSS IntelliSense suggestion.

▶ min-width
  Description: Sets the minimum allowed width.
  Syntax:      min-width: 120px;
  VSCode/Emmet: Type the Emmet abbreviation `miw` in a CSS rule and press Tab/Enter to expand it to `min-width: ;` with the cursor ready for a value, or just start typing `min-width` and accept VS Code's native CSS IntelliSense suggestion.

▶ outline
  Description: Draws a line around an element outside its border, often for focus states.
  Syntax:      outline: 2px solid blue;
  VSCode/Emmet: Type the Emmet abbreviation `ol` in a CSS rule and press Tab/Enter to expand it to `outline: ;` with the cursor ready for a value, or just start typing `outline` and accept VS Code's native CSS IntelliSense suggestion.

▶ padding
  Description: Sets space inside an element's border, around its content.
  Syntax:      padding: 12px 20px;
  VSCode/Emmet: Type the Emmet abbreviation `p` in a CSS rule and press Tab/Enter to expand it to `padding: ;` with the cursor ready for a value, or just start typing `padding` and accept VS Code's native CSS IntelliSense suggestion.

▶ width
  Description: Sets the content width of an element.
  Syntax:      width: 320px;
  VSCode/Emmet: Type the Emmet abbreviation `w` in a CSS rule and press Tab/Enter to expand it to `width: ;` with the cursor ready for a value, or just start typing `width` and accept VS Code's native CSS IntelliSense suggestion.


[Category: Display & Visibility] (5 entries)
--------------------------------------------------------------------------------
▶ display
  Description: Sets how an element generates boxes: block, inline, flex, grid, none...
  Syntax:      display: flex;
  VSCode/Emmet: Type the Emmet abbreviation `d` in a CSS rule and press Tab/Enter to expand it to `display: ;` with the cursor ready for a value, or just start typing `display` and accept VS Code's native CSS IntelliSense suggestion.

▶ overflow
  Description: Controls what happens to content that overflows its box.
  Syntax:      overflow: auto;
  VSCode/Emmet: Type the Emmet abbreviation `ov` in a CSS rule and press Tab/Enter to expand it to `overflow: ;` with the cursor ready for a value, or just start typing `overflow` and accept VS Code's native CSS IntelliSense suggestion.

▶ overflow-x
  Description: Controls horizontal overflow behavior.
  Syntax:      overflow-x: hidden;
  VSCode/Emmet: Type the Emmet abbreviation `ovx` in a CSS rule and press Tab/Enter to expand it to `overflow-x: ;` with the cursor ready for a value, or just start typing `overflow-x` and accept VS Code's native CSS IntelliSense suggestion.

▶ overflow-y
  Description: Controls vertical overflow behavior.
  Syntax:      overflow-y: scroll;
  VSCode/Emmet: Type the Emmet abbreviation `ovy` in a CSS rule and press Tab/Enter to expand it to `overflow-y: ;` with the cursor ready for a value, or just start typing `overflow-y` and accept VS Code's native CSS IntelliSense suggestion.

▶ visibility
  Description: Hides an element while it still takes up layout space.
  Syntax:      visibility: hidden;
  VSCode/Emmet: Type the Emmet abbreviation `v` in a CSS rule and press Tab/Enter to expand it to `visibility: ;` with the cursor ready for a value, or just start typing `visibility` and accept VS Code's native CSS IntelliSense suggestion.


[Category: Effects] (12 entries)
--------------------------------------------------------------------------------
▶ backdrop-filter
  Description: Glassmorphism blur effect.
  Syntax:      backdrop-filter: blur(10px);
  VSCode/Emmet: Start typing `backdrop-filter` in a CSS rule -- VS Code's native CSS IntelliSense will suggest the property; press Enter/Tab to accept it (Ctrl+Space to force the suggestion list).

▶ blur()
  Description: Applies a Gaussian blur to an element.
  Syntax:      filter: blur(4px);
  VSCode/Emmet: Inside a property value, start typing `blur()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.

▶ box-shadow
  Description: Adds a drop shadow.
  Syntax:      box-shadow: 0 4px 8px rgba(0,0,0,.1);
  VSCode/Emmet: Type the Emmet abbreviation `bxsh` in a CSS rule and press Tab/Enter to expand it to `box-shadow: ;` with the cursor ready for a value, or just start typing `box-shadow` and accept VS Code's native CSS IntelliSense suggestion.

▶ brightness()
  Description: Adjusts the brightness of an element (1 = normal, 0 = black).
  Syntax:      filter: brightness(1.2);
  VSCode/Emmet: Inside a property value, start typing `brightness()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.

▶ clip-path
  Description: Clips into a custom shape.
  Syntax:      clip-path: circle(50%);
  VSCode/Emmet: Start typing `clip-path` in a CSS rule -- VS Code's native CSS IntelliSense will suggest the property; press Enter/Tab to accept it (Ctrl+Space to force the suggestion list).

▶ contrast()
  Description: Adjusts the contrast of an element.
  Syntax:      filter: contrast(1.4);
  VSCode/Emmet: Inside a property value, start typing `contrast()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.

▶ drop-shadow()
  Description: Applies a shadow that follows an element's alpha shape (unlike box-shadow).
  Syntax:      filter: drop-shadow(0 4px 6px rgba(0,0,0,.3));
  VSCode/Emmet: Inside a property value, start typing `drop-shadow()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.

▶ grayscale()
  Description: Converts an element to grayscale (0 = normal, 1 = fully gray).
  Syntax:      filter: grayscale(1);
  VSCode/Emmet: Inside a property value, start typing `grayscale()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.

▶ hue-rotate()
  Description: Rotates the hues of an element's colors around the color wheel.
  Syntax:      filter: hue-rotate(90deg);
  VSCode/Emmet: Inside a property value, start typing `hue-rotate()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.

▶ invert()
  Description: Inverts the colors of an element.
  Syntax:      filter: invert(1);
  VSCode/Emmet: Inside a property value, start typing `invert()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.

▶ saturate()
  Description: Adjusts color saturation of an element.
  Syntax:      filter: saturate(1.8);
  VSCode/Emmet: Inside a property value, start typing `saturate()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.

▶ sepia()
  Description: Applies a sepia (vintage brown) tone to an element.
  Syntax:      filter: sepia(0.6);
  VSCode/Emmet: Inside a property value, start typing `sepia()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.


[Category: Emmet] (21 entries)
--------------------------------------------------------------------------------
▶ ai:c
  Description: align-items: center;
  Syntax:      align-items: center;
  VSCode/Emmet: Type `ai:c` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ br:50p
  Description: border-radius: 50%; (circle)
  Syntax:      border-radius: 50%;
  VSCode/Emmet: Type `br:50p` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ bxsh:r
  Description: Red box shadow shorthand.
  Syntax:      box-shadow: 0 0 0 red;
  VSCode/Emmet: Type `bxsh:r` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ bxsz:bb
  Description: box-sizing: border-box;
  Syntax:      box-sizing: border-box;
  VSCode/Emmet: Type `bxsz:bb` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ cur:p
  Description: cursor: pointer;
  Syntax:      cursor: pointer;
  VSCode/Emmet: Type `cur:p` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ df
  Description: display: flex;
  Syntax:      display: flex;
  VSCode/Emmet: Type `df` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ dg
  Description: display: grid;
  Syntax:      display: grid;
  VSCode/Emmet: Type `dg` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ fw:b
  Description: font-weight: bold;
  Syntax:      font-weight: bold;
  VSCode/Emmet: Type `fw:b` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ jc:c
  Description: justify-content: center;
  Syntax:      justify-content: center;
  VSCode/Emmet: Type `jc:c` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ m0
  Description: margin: 0;
  Syntax:      margin: 0;
  VSCode/Emmet: Type `m0` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ m10a
  Description: Auto margin shorthand.
  Syntax:      margin: 10px auto;
  VSCode/Emmet: Type `m10a` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ op:0
  Description: opacity: 0;
  Syntax:      opacity: 0;
  VSCode/Emmet: Type `op:0` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ ov:h
  Description: overflow: hidden;
  Syntax:      overflow: hidden;
  VSCode/Emmet: Type `ov:h` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ p0
  Description: padding: 0;
  Syntax:      padding: 0;
  VSCode/Emmet: Type `p0` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ pos:f
  Description: position: fixed;
  Syntax:      position: fixed;
  VSCode/Emmet: Type `pos:f` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ pos:r
  Description: position: relative;
  Syntax:      position: relative;
  VSCode/Emmet: Type `pos:r` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ posa
  Description: position: absolute;
  Syntax:      position: absolute;
  VSCode/Emmet: Type `posa` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ ta:c
  Description: text-align: center;
  Syntax:      text-align: center;
  VSCode/Emmet: Type `ta:c` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ trf:rx
  Description: transform: rotate(angle);
  Syntax:      transform: rotate(45deg);
  VSCode/Emmet: Type `trf:rx` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ tt:u
  Description: text-transform: uppercase;
  Syntax:      text-transform: uppercase;
  VSCode/Emmet: Type `tt:u` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.

▶ wh:cover
  Description: background-size: cover shorthand alias.
  Syntax:      background-size: cover;
  VSCode/Emmet: Type `wh:cover` in a stylesheet file, then press Tab (with "emmet.triggerExpansionOnTab": true) or Enter on the Emmet suggestion to expand it in place.


[Category: Flexbox] (11 entries)
--------------------------------------------------------------------------------
▶ align-content
  Description: Aligns flex lines within the container when there is extra space.
  Syntax:      align-content: space-between;
  VSCode/Emmet: Type the Emmet abbreviation `ac` in a CSS rule and press Tab/Enter to expand it to `align-content: ;` with the cursor ready for a value, or just start typing `align-content` and accept VS Code's native CSS IntelliSense suggestion.

▶ align-items
  Description: Aligns flex items along the cross axis of the current line of the flex container.
  Syntax:      align-items: stretch | flex-start | flex-end | center | baseline;
  VSCode/Emmet: Type the Emmet abbreviation `ai` in a CSS rule and press Tab/Enter to expand it to `align-items: ;` with the cursor ready for a value, or just start typing `align-items` and accept VS Code's native CSS IntelliSense suggestion.

▶ align-self
  Description: Overrides align-items for a single flex/grid item.
  Syntax:      align-self: flex-end;
  VSCode/Emmet: Type the Emmet abbreviation `as` in a CSS rule and press Tab/Enter to expand it to `align-self: ;` with the cursor ready for a value, or just start typing `align-self` and accept VS Code's native CSS IntelliSense suggestion.

▶ flex-basis
  Description: Sets the initial main size of a flex item.
  Syntax:      flex-basis: 200px;
  VSCode/Emmet: Type the Emmet abbreviation `fxb` in a CSS rule and press Tab/Enter to expand it to `flex-basis: ;` with the cursor ready for a value, or just start typing `flex-basis` and accept VS Code's native CSS IntelliSense suggestion.

▶ flex-direction
  Description: Defines the main-axis direction of the flex container, arranging items horizontally or vertically.
  Syntax:      flex-direction: row | row-reverse | column | column-reverse;
  VSCode/Emmet: Type the Emmet abbreviation `fxd` in a CSS rule and press Tab/Enter to expand it to `flex-direction: ;` with the cursor ready for a value, or just start typing `flex-direction` and accept VS Code's native CSS IntelliSense suggestion.

▶ flex-grow
  Description: Sets how much a flex item grows relative to its siblings.
  Syntax:      flex-grow: 1;
  VSCode/Emmet: Type the Emmet abbreviation `fxg` in a CSS rule and press Tab/Enter to expand it to `flex-grow: ;` with the cursor ready for a value, or just start typing `flex-grow` and accept VS Code's native CSS IntelliSense suggestion.

▶ flex-shrink
  Description: Sets how much a flex item shrinks relative to its siblings.
  Syntax:      flex-shrink: 0;
  VSCode/Emmet: Type the Emmet abbreviation `fxsh` in a CSS rule and press Tab/Enter to expand it to `flex-shrink: ;` with the cursor ready for a value, or just start typing `flex-shrink` and accept VS Code's native CSS IntelliSense suggestion.

▶ flex-wrap
  Description: Controls whether the flex container forces all items onto a single line or allows wrapping across multiple lines.
  Syntax:      flex-wrap: nowrap | wrap | wrap-reverse;
  VSCode/Emmet: Type the Emmet abbreviation `fxw` in a CSS rule and press Tab/Enter to expand it to `flex-wrap: ;` with the cursor ready for a value, or just start typing `flex-wrap` and accept VS Code's native CSS IntelliSense suggestion.

▶ flex: 1
  Description: Grow to fill available space.
  Syntax:      flex: 1;
  VSCode/Emmet: Type the Emmet abbreviation `fx` in a CSS rule and press Tab/Enter to expand it to `flex: ;` with the cursor ready for a value, or just start typing `flex` and accept VS Code's native CSS IntelliSense suggestion.

▶ gap: 1rem
  Description: Space between flex/grid children.
  Syntax:      display: flex;
  VSCode/Emmet: Type the Emmet abbreviation `g` in a CSS rule and press Tab/Enter to expand it to `gap: ;` with the cursor ready for a value, or just start typing `gap` and accept VS Code's native CSS IntelliSense suggestion.

▶ justify-content
  Description: Aligns flex items along the main axis of the current line of the flex container.
  Syntax:      justify-content: flex-start | flex-end | center | space-between | space-around | space-evenly;
  VSCode/Emmet: Type the Emmet abbreviation `jc` in a CSS rule and press Tab/Enter to expand it to `justify-content: ;` with the cursor ready for a value, or just start typing `justify-content` and accept VS Code's native CSS IntelliSense suggestion.


[Category: Grid] (8 entries)
--------------------------------------------------------------------------------
▶ gap
  Description: Sets spacing between grid/flex rows and columns.
  Syntax:      gap: 16px;
  VSCode/Emmet: Type the Emmet abbreviation `g` in a CSS rule and press Tab/Enter to expand it to `gap: ;` with the cursor ready for a value, or just start typing `gap` and accept VS Code's native CSS IntelliSense suggestion.

▶ grid-area
  Description: Places an item into a named grid area.
  Syntax:      grid-area: header;
  VSCode/Emmet: Type the Emmet abbreviation `ga` in a CSS rule and press Tab/Enter to expand it to `grid-area: ;` with the cursor ready for a value, or just start typing `grid-area` and accept VS Code's native CSS IntelliSense suggestion.

▶ grid-column
  Description: Shorthand to place an item across grid columns.
  Syntax:      grid-column: 1 / 3;
  VSCode/Emmet: Type the Emmet abbreviation `gc` in a CSS rule and press Tab/Enter to expand it to `grid-column: ;` with the cursor ready for a value, or just start typing `grid-column` and accept VS Code's native CSS IntelliSense suggestion.

▶ grid-row
  Description: Shorthand to place an item across grid rows.
  Syntax:      grid-row: 1 / 2;
  VSCode/Emmet: Type the Emmet abbreviation `gr` in a CSS rule and press Tab/Enter to expand it to `grid-row: ;` with the cursor ready for a value, or just start typing `grid-row` and accept VS Code's native CSS IntelliSense suggestion.

▶ grid-template-areas
  Description: Names grid regions for placing items by area name.
  Syntax:      grid-template-areas: "header header" "sidebar main";
  VSCode/Emmet: Type the Emmet abbreviation `gta` in a CSS rule and press Tab/Enter to expand it to `grid-template-areas: ;` with the cursor ready for a value, or just start typing `grid-template-areas` and accept VS Code's native CSS IntelliSense suggestion.

▶ grid-template-columns
  Description: Defines the size and number of grid columns.
  Syntax:      grid-template-columns: repeat(3, 1fr);
  VSCode/Emmet: Type the Emmet abbreviation `gtc` in a CSS rule and press Tab/Enter to expand it to `grid-template-columns: ;` with the cursor ready for a value, or just start typing `grid-template-columns` and accept VS Code's native CSS IntelliSense suggestion.

▶ grid-template-rows
  Description: Defines the size and number of grid rows.
  Syntax:      grid-template-rows: auto 1fr;
  VSCode/Emmet: Type the Emmet abbreviation `gtr` in a CSS rule and press Tab/Enter to expand it to `grid-template-rows: ;` with the cursor ready for a value, or just start typing `grid-template-rows` and accept VS Code's native CSS IntelliSense suggestion.

▶ subgrid
  Description: Lets a nested grid inherit the row/column tracks of its parent grid.
  Syntax:      grid-template-columns: subgrid;
  VSCode/Emmet: Start typing `subgrid` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.


[Category: Interaction] (4 entries)
--------------------------------------------------------------------------------
▶ cursor
  Description: Sets the mouse cursor type to display when hovering over an element, supporting custom asset fallback URLs.
  Syntax:      cursor: [url(<image_path>) x_offset y_offset,]* [keyword];
  VSCode/Emmet: Type `cursor` in a CSS rule. VS Code suggests `url()` or keywords like `pointer`, `grab`, and `crosshair`.

▶ cursor: grab / grabbing
  Description: Renders hand-state cursors signaling that an item can be clicked and dragged.
  Syntax:      cursor: grab;
cursor: grabbing;
  VSCode/Emmet: Start typing `grab` or `grabbing` inside the cursor value slot to autocomplete.

▶ cursor: options
  Description: Interactive list and index of standard CSS mouse cursor styles.
  Syntax:      cursor: pointer | grab | crosshair | text | help | wait | zoom-in | not-allowed;
  VSCode/Emmet: Type `cursor:` to trigger the full suggestions list of keywords in VS Code.

▶ cursor: zoom-in / zoom-out
  Description: Displays magnifying glass cursors signaling zoom actions on images.
  Syntax:      cursor: zoom-in;
cursor: zoom-out;
  VSCode/Emmet: Autocomplete `zoom-in` or `zoom-out` inside the `cursor:` value list.


[Category: Layout] (13 entries)
--------------------------------------------------------------------------------
▶ @container
  Description: Defines a container query context so children can respond to the container's size.
  Syntax:      @container (min-width: 400px) {
  VSCode/Emmet: Type `@container` at the top level of a stylesheet -- VS Code's CSS language service recognizes at-rules and will suggest them; press Enter/Tab to accept, then Ctrl+Space inside the block for further suggestions.

▶ aspect-ratio
  Description: Forces a width:height ratio.
  Syntax:      aspect-ratio: 16 / 9;
  VSCode/Emmet: Type the Emmet abbreviation `ar` in a CSS rule and press Tab/Enter to expand it to `aspect-ratio: ;` with the cursor ready for a value, or just start typing `aspect-ratio` and accept VS Code's native CSS IntelliSense suggestion.

▶ calc()
  Description: Performs calculations mixing different units, e.g. percentages and pixels.
  Syntax:      width: calc(100% - 40px);
  VSCode/Emmet: Inside a property value, start typing `calc()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.

▶ clamp()
  Description: Clamps a value between a minimum and maximum, scaling with a preferred value.
  Syntax:      font-size: clamp(1rem, 2vw, 1.5rem);
  VSCode/Emmet: Inside a property value, start typing `clamp()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.

▶ container-type
  Description: Opts an element in as a query container for @container rules.
  Syntax:      container-type: inline-size;
  VSCode/Emmet: Start typing `container-type` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ display: grid
  Description: 2D layout system.
  Syntax:      display: grid;
  VSCode/Emmet: Type the Emmet abbreviation `d` in a CSS rule and press Tab/Enter to expand it to `display: ;` with the cursor ready for a value, or just start typing `display` and accept VS Code's native CSS IntelliSense suggestion.

▶ max()
  Description: Uses the largest of a list of values.
  Syntax:      padding: max(16px, 5%);
  VSCode/Emmet: Inside a property value, start typing `max()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.

▶ min()
  Description: Uses the smallest of a list of values.
  Syntax:      width: min(90%, 600px);
  VSCode/Emmet: Inside a property value, start typing `min()` -- VS Code's CSS IntelliSense will suggest it as a value completion; press Enter/Tab to accept.

▶ place-items: center
  Description: Centers content in a grid.
  Syntax:      display: grid;
  VSCode/Emmet: Start typing `place-items` in a CSS rule -- VS Code's native CSS IntelliSense will suggest the property; press Enter/Tab to accept it (Ctrl+Space to force the suggestion list).

▶ position: sticky
  Description: Sticks while scrolling within parent.
  Syntax:      position: sticky;
  VSCode/Emmet: Type the Emmet abbreviation `pos` in a CSS rule and press Tab/Enter to expand it to `position: ;` with the cursor ready for a value, or just start typing `position` and accept VS Code's native CSS IntelliSense suggestion.

▶ scroll-snap-align
  Description: Sets where a child snaps to within its scroll-snap container.
  Syntax:      scroll-snap-align: center;
  VSCode/Emmet: Start typing `scroll-snap-align` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ scroll-snap-type
  Description: Enables scroll snapping on a scroll container along an axis.
  Syntax:      scroll-snap-type: x mandatory;
  VSCode/Emmet: Start typing `scroll-snap-type` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ writing-mode
  Description: Sets whether text flows horizontally or vertically.
  Syntax:      writing-mode: vertical-rl;
  VSCode/Emmet: Start typing `writing-mode` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.


[Category: Lists & Tables] (4 entries)
--------------------------------------------------------------------------------
▶ border-collapse
  Description: Merges or separates table cell borders.
  Syntax:      border-collapse: collapse;
  VSCode/Emmet: Type the Emmet abbreviation `bdcl` in a CSS rule and press Tab/Enter to expand it to `border-collapse: ;` with the cursor ready for a value, or just start typing `border-collapse` and accept VS Code's native CSS IntelliSense suggestion.

▶ list-style
  Description: Shorthand for list marker type, position, and image.
  Syntax:      list-style: none;
  VSCode/Emmet: Type the Emmet abbreviation `lis` in a CSS rule and press Tab/Enter to expand it to `list-style: ;` with the cursor ready for a value, or just start typing `list-style` and accept VS Code's native CSS IntelliSense suggestion.

▶ list-style-type
  Description: Sets the marker shape/type for list items.
  Syntax:      list-style-type: square;
  VSCode/Emmet: Type the Emmet abbreviation `list` in a CSS rule and press Tab/Enter to expand it to `list-style-type: ;` with the cursor ready for a value, or just start typing `list-style-type` and accept VS Code's native CSS IntelliSense suggestion.

▶ table-layout
  Description: Sets the table layout algorithm: auto or fixed.
  Syntax:      table-layout: fixed;
  VSCode/Emmet: Type the Emmet abbreviation `tbl` in a CSS rule and press Tab/Enter to expand it to `table-layout: ;` with the cursor ready for a value, or just start typing `table-layout` and accept VS Code's native CSS IntelliSense suggestion.


[Category: Positioning] (8 entries)
--------------------------------------------------------------------------------
▶ bottom
  Description: Offsets a positioned element from the bottom edge.
  Syntax:      bottom: 0;
  VSCode/Emmet: Type the Emmet abbreviation `b` in a CSS rule and press Tab/Enter to expand it to `bottom: ;` with the cursor ready for a value, or just start typing `bottom` and accept VS Code's native CSS IntelliSense suggestion.

▶ clear
  Description: Stops an element from wrapping around floated elements.
  Syntax:      clear: both;
  VSCode/Emmet: Type the Emmet abbreviation `cl` in a CSS rule and press Tab/Enter to expand it to `clear: ;` with the cursor ready for a value, or just start typing `clear` and accept VS Code's native CSS IntelliSense suggestion.

▶ float
  Description: Floats an element to the left or right of its container.
  Syntax:      float: left;
  VSCode/Emmet: Type the Emmet abbreviation `fl` in a CSS rule and press Tab/Enter to expand it to `float: ;` with the cursor ready for a value, or just start typing `float` and accept VS Code's native CSS IntelliSense suggestion.

▶ left
  Description: Offsets a positioned element from the left edge.
  Syntax:      left: 0;
  VSCode/Emmet: Type the Emmet abbreviation `l` in a CSS rule and press Tab/Enter to expand it to `left: ;` with the cursor ready for a value, or just start typing `left` and accept VS Code's native CSS IntelliSense suggestion.

▶ position
  Description: Sets how an element is positioned: static, relative, absolute, fixed, sticky.
  Syntax:      position: relative;
  VSCode/Emmet: Type the Emmet abbreviation `pos` in a CSS rule and press Tab/Enter to expand it to `position: ;` with the cursor ready for a value, or just start typing `position` and accept VS Code's native CSS IntelliSense suggestion.

▶ right
  Description: Offsets a positioned element from the right edge.
  Syntax:      right: 0;
  VSCode/Emmet: Type the Emmet abbreviation `r` in a CSS rule and press Tab/Enter to expand it to `right: ;` with the cursor ready for a value, or just start typing `right` and accept VS Code's native CSS IntelliSense suggestion.

▶ top
  Description: Offsets a positioned element from the top edge.
  Syntax:      top: 0;
  VSCode/Emmet: Type the Emmet abbreviation `t` in a CSS rule and press Tab/Enter to expand it to `top: ;` with the cursor ready for a value, or just start typing `top` and accept VS Code's native CSS IntelliSense suggestion.

▶ z-index
  Description: Controls the stacking order of positioned elements.
  Syntax:      z-index: 10;
  VSCode/Emmet: Type the Emmet abbreviation `z` in a CSS rule and press Tab/Enter to expand it to `z-index: ;` with the cursor ready for a value, or just start typing `z-index` and accept VS Code's native CSS IntelliSense suggestion.


[Category: Responsive] (2 entries)
--------------------------------------------------------------------------------
▶ @media
  Description: Conditional styles by viewport.
  Syntax:      @media (max-width: 768px) {
  VSCode/Emmet: Start typing `@media` in a CSS rule -- VS Code's native CSS IntelliSense will suggest the property; press Enter/Tab to accept it (Ctrl+Space to force the suggestion list).

▶ @supports
  Description: Applies CSS rules only if the browser supports a given feature.
  Syntax:      @supports (display: grid) {
  VSCode/Emmet: Type `@supports` at the top level of a stylesheet -- VS Code's CSS language service recognizes at-rules and will suggest them; press Enter/Tab to accept, then Ctrl+Space inside the block for further suggestions.


[Category: Scrollbar] (1 entries)
--------------------------------------------------------------------------------
▶ ::-webkit-scrollbar
  Description: Custom scrollbar styling.
  Syntax:      ::-webkit-scrollbar { width: 10px; }
  VSCode/Emmet: Start typing `` in a CSS rule -- VS Code's native CSS IntelliSense will suggest the property; press Enter/Tab to accept it (Ctrl+Space to force the suggestion list).


[Category: Selectors] (24 entries)
--------------------------------------------------------------------------------
▶ ::after
  Description: Inserts generated content after an element's actual content.
  Syntax:      .tooltip::after { content: attr(data-tip); }
  VSCode/Emmet: VS Code's CSS language service lists `::after` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ ::before
  Description: Inserts generated content before an element's actual content.
  Syntax:      .icon::before { content: '★'; }
  VSCode/Emmet: VS Code's CSS language service lists `::before` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ ::placeholder
  Description: Styles the placeholder text of a form field.
  Syntax:      input::placeholder { color: #999; }
  VSCode/Emmet: VS Code's CSS language service lists `::placeholder` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ :active
  Description: Matches an element while it is being activated/pressed.
  Syntax:      button:active { transform: scale(0.98); }
  VSCode/Emmet: VS Code's CSS language service lists `:active` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ :checked
  Description: Matches a checkbox, radio, or option that is currently checked/selected.
  Syntax:      input:checked + label { font-weight: bold; }
  VSCode/Emmet: VS Code's CSS language service lists `:checked` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ :disabled
  Description: Matches form elements that are currently disabled.
  Syntax:      input:disabled { opacity: 0.5; }
  VSCode/Emmet: VS Code's CSS language service lists `:disabled` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ :empty
  Description: Matches elements that have no children (including no text).
  Syntax:      p:empty { display: none; }
  VSCode/Emmet: VS Code's CSS language service lists `:empty` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ :first-child
  Description: Matches an element that is the first child of its parent.
  Syntax:      li:first-child { font-weight: bold; }
  VSCode/Emmet: VS Code's CSS language service lists `:first-child` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ :focus
  Description: Matches an element while it has keyboard focus.
  Syntax:      input:focus { outline: 2px solid blue; }
  VSCode/Emmet: VS Code's CSS language service lists `:focus` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ :focus-visible
  Description: Matches focus only when it should be visibly indicated (e.g. keyboard nav).
  Syntax:      :focus-visible { outline: 2px solid; }
  VSCode/Emmet: VS Code's CSS language service lists `:focus-visible` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ :has()
  Description: Matches an element only if it contains a descendant matching the given selector.
  Syntax:      card:has(img) { padding-top: 0; }
  VSCode/Emmet: VS Code's CSS language service lists `:has()` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ :hover
  Description: Matches an element while the pointer is hovering over it.
  Syntax:      a:hover { color: red; }
  VSCode/Emmet: VS Code's CSS language service lists `:hover` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ :is()
  Description: Matches any selector in a list, reducing repetition in complex selectors.
  Syntax:      is(header, footer) a { color: teal; }
  VSCode/Emmet: VS Code's CSS language service lists `:is()` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ :last-child
  Description: Matches an element that is the last child of its parent.
  Syntax:      li:last-child { border: none; }
  VSCode/Emmet: VS Code's CSS language service lists `:last-child` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ :not()
  Description: Excludes elements that match the given selector.
  Syntax:      li:not(.active) { opacity: 0.5; }
  VSCode/Emmet: VS Code's CSS language service lists `:not()` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ :nth-child(odd)
  Description: Targets alternating elements.
  Syntax:      li:nth-child(odd) { background: #f5f5f5; }
  VSCode/Emmet: VS Code's CSS language service lists `:nth-child(odd)` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ :nth-of-type()
  Description: Matches elements based on their position among siblings of the same type.
  Syntax:      p:nth-of-type(2) { color: gray; }
  VSCode/Emmet: VS Code's CSS language service lists `:nth-of-type()` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ :target
  Description: Matches the element whose id matches the URL's current fragment (#id).
  Syntax:      #section1:target { background: yellow; }
  VSCode/Emmet: VS Code's CSS language service lists `:target` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ :where()
  Description: Like :is(), but always contributes zero specificity -- easy to override.
  Syntax:      where(.card, .panel) p { margin: 0; }
  VSCode/Emmet: VS Code's CSS language service lists `:where()` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ [attr=value]
  Description: Matches elements whose attribute equals a given value.
  Syntax:      input[type="email"] { border-color: blue; }
  VSCode/Emmet: VS Code's CSS language service lists `[attr=value]` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ &:hover (SCSS)
  Description: Nested pseudo-class in Sass.
  Syntax:      .btn {
  VSCode/Emmet: VS Code's CSS language service lists `&:hover (SCSS)` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ + (adjacent sibling)
  Description: Combinator matching the element immediately following another.
  Syntax:      h2 + p { font-size: 1.1em; }
  VSCode/Emmet: VS Code's CSS language service lists `+ (adjacent sibling)` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ >  (child)
  Description: Combinator matching direct children only.
  Syntax:      ul > li { list-style: none; }
  VSCode/Emmet: VS Code's CSS language service lists `>  (child)` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.

▶ ~ (general sibling)
  Description: Combinator matching any later sibling that shares a parent.
  Syntax:      h2 ~ p { margin-top: 0; }
  VSCode/Emmet: VS Code's CSS language service lists `~ (general sibling)` in the completion popup as you type inside a stylesheet; select it and press Enter. There is no Emmet shortcut for pseudo-selectors -- they are typed directly.


[Category: Typography] (15 entries)
--------------------------------------------------------------------------------
▶ font-family
  Description: Sets the typeface(s) used for text.
  Syntax:      font-family: 'Inter', sans-serif;
  VSCode/Emmet: Type the Emmet abbreviation `ff` in a CSS rule and press Tab/Enter to expand it to `font-family: ;` with the cursor ready for a value, or just start typing `font-family` and accept VS Code's native CSS IntelliSense suggestion.

▶ font-size
  Description: Sets the size of text.
  Syntax:      font-size: 1.125rem;
  VSCode/Emmet: Type the Emmet abbreviation `fsz` in a CSS rule and press Tab/Enter to expand it to `font-size: ;` with the cursor ready for a value, or just start typing `font-size` and accept VS Code's native CSS IntelliSense suggestion.

▶ font-style
  Description: Sets italic/oblique/normal text style.
  Syntax:      font-style: italic;
  VSCode/Emmet: Type the Emmet abbreviation `fs` in a CSS rule and press Tab/Enter to expand it to `font-style: ;` with the cursor ready for a value, or just start typing `font-style` and accept VS Code's native CSS IntelliSense suggestion.

▶ font-weight
  Description: Sets the thickness/boldness of text.
  Syntax:      font-weight: 600;
  VSCode/Emmet: Type the Emmet abbreviation `fw` in a CSS rule and press Tab/Enter to expand it to `font-weight: ;` with the cursor ready for a value, or just start typing `font-weight` and accept VS Code's native CSS IntelliSense suggestion.

▶ hyphens
  Description: Enables automatic hyphenation of text at line breaks.
  Syntax:      hyphens: auto;
  VSCode/Emmet: Start typing `hyphens` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ letter-spacing
  Description: Adjusts spacing between characters.
  Syntax:      letter-spacing: 0.05em;
  VSCode/Emmet: Type the Emmet abbreviation `lts` in a CSS rule and press Tab/Enter to expand it to `letter-spacing: ;` with the cursor ready for a value, or just start typing `letter-spacing` and accept VS Code's native CSS IntelliSense suggestion.

▶ line-clamp
  Description: Truncates to N lines.
  Syntax:      display: -webkit-box;
  VSCode/Emmet: Start typing `line-clamp` in a CSS rule -- VS Code's native CSS IntelliSense will suggest the property; press Enter/Tab to accept it (Ctrl+Space to force the suggestion list).

▶ line-height
  Description: Sets the height of a line of text.
  Syntax:      line-height: 1.5;
  VSCode/Emmet: Type the Emmet abbreviation `lh` in a CSS rule and press Tab/Enter to expand it to `line-height: ;` with the cursor ready for a value, or just start typing `line-height` and accept VS Code's native CSS IntelliSense suggestion.

▶ text-align
  Description: Aligns text horizontally within its container.
  Syntax:      text-align: center;
  VSCode/Emmet: Type the Emmet abbreviation `ta` in a CSS rule and press Tab/Enter to expand it to `text-align: ;` with the cursor ready for a value, or just start typing `text-align` and accept VS Code's native CSS IntelliSense suggestion.

▶ text-decoration
  Description: Adds/removes underline, overline, or strikethrough.
  Syntax:      text-decoration: underline;
  VSCode/Emmet: Type the Emmet abbreviation `td` in a CSS rule and press Tab/Enter to expand it to `text-decoration: ;` with the cursor ready for a value, or just start typing `text-decoration` and accept VS Code's native CSS IntelliSense suggestion.

▶ text-indent
  Description: Indents the first line of a text block.
  Syntax:      text-indent: 2em;
  VSCode/Emmet: Type the Emmet abbreviation `ti` in a CSS rule and press Tab/Enter to expand it to `text-indent: ;` with the cursor ready for a value, or just start typing `text-indent` and accept VS Code's native CSS IntelliSense suggestion.

▶ text-overflow: ellipsis
  Description: Truncates overflowing text.
  Syntax:      overflow: hidden;
  VSCode/Emmet: Type the Emmet abbreviation `tov` in a CSS rule and press Tab/Enter to expand it to `text-overflow: ;` with the cursor ready for a value, or just start typing `text-overflow` and accept VS Code's native CSS IntelliSense suggestion.

▶ text-wrap
  Description: Controls text wrapping behavior, e.g. balance for even line lengths in headings.
  Syntax:      text-wrap: balance;
  VSCode/Emmet: Start typing `text-wrap` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ vertical-align
  Description: Aligns inline/table-cell content vertically.
  Syntax:      vertical-align: middle;
  VSCode/Emmet: Type the Emmet abbreviation `va` in a CSS rule and press Tab/Enter to expand it to `vertical-align: ;` with the cursor ready for a value, or just start typing `vertical-align` and accept VS Code's native CSS IntelliSense suggestion.

▶ white-space
  Description: Controls how whitespace and line breaks are handled.
  Syntax:      white-space: nowrap;
  VSCode/Emmet: Type the Emmet abbreviation `whs` in a CSS rule and press Tab/Enter to expand it to `white-space: ;` with the cursor ready for a value, or just start typing `white-space` and accept VS Code's native CSS IntelliSense suggestion.


[Category: Variables] (2 entries)
--------------------------------------------------------------------------------
▶ --custom-prop
  Description: Reusable CSS custom properties.
  Syntax:      :root { --brand: #6c5ce7; }
  VSCode/Emmet: Start typing `--custom-prop` in a CSS rule -- VS Code's native CSS IntelliSense will suggest the property; press Enter/Tab to accept it (Ctrl+Space to force the suggestion list).

▶ @property
  Description: Registers a custom property with a type, initial value, and inheritance behavior.
  Syntax:      @property --angle {
  VSCode/Emmet: Type `@property` at the top level of a stylesheet -- VS Code's CSS language service recognizes at-rules and will suggest them; press Enter/Tab to accept, then Ctrl+Space inside the block for further suggestions.


[Category: Vendor Prefixes] (10 entries)
--------------------------------------------------------------------------------
▶ -moz-appearance
  Description: Firefox-prefixed way to remove native styling from form controls.
  Syntax:      -moz-appearance: none;
  VSCode/Emmet: VS Code's CSS IntelliSense lists vendor-prefixed properties like `-moz-appearance` in the completion popup alongside the unprefixed version -- start typing and select it, or use an Autoprefixer/PostCSS build step so you only have to write the standard property.

▶ -ms-overflow-style
  Description: Legacy Internet Explorer/Edge property to hide the scrollbar.
  Syntax:      -ms-overflow-style: none;
  VSCode/Emmet: VS Code's CSS IntelliSense lists vendor-prefixed properties like `-ms-overflow-style` in the completion popup alongside the unprefixed version -- start typing and select it, or use an Autoprefixer/PostCSS build step so you only have to write the standard property.

▶ -webkit-appearance
  Description: Removes/customizes native OS styling of form controls in WebKit/Blink browsers.
  Syntax:      -webkit-appearance: none;
  VSCode/Emmet: VS Code's CSS IntelliSense lists vendor-prefixed properties like `-webkit-appearance` in the completion popup alongside the unprefixed version -- start typing and select it, or use an Autoprefixer/PostCSS build step so you only have to write the standard property.

▶ -webkit-backdrop-filter
  Description: WebKit/Safari-specific prefix required for backdrop-filter to work on older Safari.
  Syntax:      -webkit-backdrop-filter: blur(10px);
  VSCode/Emmet: VS Code's CSS IntelliSense lists vendor-prefixed properties like `-webkit-backdrop-filter` in the completion popup alongside the unprefixed version -- start typing and select it, or use an Autoprefixer/PostCSS build step so you only have to write the standard property.

▶ -webkit-line-clamp
  Description: Truncates multi-line text with an ellipsis; requires display:-webkit-box in WebKit/Blink.
  Syntax:      display: -webkit-box;
  VSCode/Emmet: VS Code's CSS IntelliSense lists vendor-prefixed properties like `-webkit-line-clamp` in the completion popup alongside the unprefixed version -- start typing and select it, or use an Autoprefixer/PostCSS build step so you only have to write the standard property.

▶ -webkit-mask
  Description: Applies a WebKit-prefixed mask image/gradient to an element (needed for older Safari).
  Syntax:      -webkit-mask-image: linear-gradient(black, transparent);
  VSCode/Emmet: VS Code's CSS IntelliSense lists vendor-prefixed properties like `-webkit-mask` in the completion popup alongside the unprefixed version -- start typing and select it, or use an Autoprefixer/PostCSS build step so you only have to write the standard property.

▶ -webkit-overflow-scrolling
  Description: Enables momentum (inertial) scrolling on older iOS Safari.
  Syntax:      -webkit-overflow-scrolling: touch;
  VSCode/Emmet: VS Code's CSS IntelliSense lists vendor-prefixed properties like `-webkit-overflow-scrolling` in the completion popup alongside the unprefixed version -- start typing and select it, or use an Autoprefixer/PostCSS build step so you only have to write the standard property.

▶ -webkit-tap-highlight-color
  Description: Removes/customizes the gray flash shown when tapping links on mobile WebKit.
  Syntax:      -webkit-tap-highlight-color: transparent;
  VSCode/Emmet: VS Code's CSS IntelliSense lists vendor-prefixed properties like `-webkit-tap-highlight-color` in the completion popup alongside the unprefixed version -- start typing and select it, or use an Autoprefixer/PostCSS build step so you only have to write the standard property.

▶ -webkit-text-fill-color
  Description: Sets the fill color of text, often combined with background-clip:text for gradient text.
  Syntax:      background: linear-gradient(90deg,#f06,#4af);
  VSCode/Emmet: VS Code's CSS IntelliSense lists vendor-prefixed properties like `-webkit-text-fill-color` in the completion popup alongside the unprefixed version -- start typing and select it, or use an Autoprefixer/PostCSS build step so you only have to write the standard property.

▶ -webkit-user-select
  Description: WebKit-prefixed way to disable/enable text selection (needed for older Safari/iOS).
  Syntax:      -webkit-user-select: none;
  VSCode/Emmet: VS Code's CSS IntelliSense lists vendor-prefixed properties like `-webkit-user-select` in the completion popup alongside the unprefixed version -- start typing and select it, or use an Autoprefixer/PostCSS build step so you only have to write the standard property.


[Category: Visual] (22 entries)
--------------------------------------------------------------------------------
▶ accent-color
  Description: Sets the accent color used by native form controls (checkboxes, radios, etc.).
  Syntax:      accent-color: #7c5cff;
  VSCode/Emmet: Start typing `accent-color` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ background-blend-mode
  Description: Specifies how background layers (images, gradients, solid colors) blend together.
  Syntax:      background-blend-mode: normal | multiply | screen | overlay | darken | lighten | color-dodge | color-burn | hard-light | soft-light | difference | exclusion | hue | saturation | color | luminosity;
  VSCode/Emmet: Type `background-blend-mode` in a CSS block; VS Code will display completion options for all standard blend keywords.

▶ background-color
  Description: Sets the background color of an element.
  Syntax:      background-color: #f5f1ed;
  VSCode/Emmet: Type the Emmet abbreviation `bgc` in a CSS rule and press Tab/Enter to expand it to `background-color: ;` with the cursor ready for a value, or just start typing `background-color` and accept VS Code's native CSS IntelliSense suggestion.

▶ background-image
  Description: Sets one or more background images.
  Syntax:      background-image: url('bg.png');
  VSCode/Emmet: Type the Emmet abbreviation `bgi` in a CSS rule and press Tab/Enter to expand it to `background-image: ;` with the cursor ready for a value, or just start typing `background-image` and accept VS Code's native CSS IntelliSense suggestion.

▶ background-position
  Description: Positions a background image within its box.
  Syntax:      background-position: center;
  VSCode/Emmet: Type the Emmet abbreviation `bgp` in a CSS rule and press Tab/Enter to expand it to `background-position: ;` with the cursor ready for a value, or just start typing `background-position` and accept VS Code's native CSS IntelliSense suggestion.

▶ background-repeat
  Description: Controls how a background image repeats.
  Syntax:      background-repeat: no-repeat;
  VSCode/Emmet: Type the Emmet abbreviation `bgr` in a CSS rule and press Tab/Enter to expand it to `background-repeat: ;` with the cursor ready for a value, or just start typing `background-repeat` and accept VS Code's native CSS IntelliSense suggestion.

▶ background-size
  Description: Sizes a background image, e.g. cover or contain.
  Syntax:      background-size: cover;
  VSCode/Emmet: Type the Emmet abbreviation `bgsz` in a CSS rule and press Tab/Enter to expand it to `background-size: ;` with the cursor ready for a value, or just start typing `background-size` and accept VS Code's native CSS IntelliSense suggestion.

▶ border
  Description: Shorthand for border-width, style, and color.
  Syntax:      border: 1px solid #ccc;
  VSCode/Emmet: Type the Emmet abbreviation `bd` in a CSS rule and press Tab/Enter to expand it to `border: ;` with the cursor ready for a value, or just start typing `border` and accept VS Code's native CSS IntelliSense suggestion.

▶ border-radius
  Description: Rounds the corners of an element's box.
  Syntax:      border-radius: 8px;
  VSCode/Emmet: Type the Emmet abbreviation `bdrs` in a CSS rule and press Tab/Enter to expand it to `border-radius: ;` with the cursor ready for a value, or just start typing `border-radius` and accept VS Code's native CSS IntelliSense suggestion.

▶ caret-color
  Description: Sets the color of the text input cursor (caret).
  Syntax:      caret-color: #ff5f56;
  VSCode/Emmet: Start typing `caret-color` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ color
  Description: Sets the text (foreground) color.
  Syntax:      color: #1a1a1a;
  VSCode/Emmet: Type the Emmet abbreviation `c` in a CSS rule and press Tab/Enter to expand it to `color: ;` with the cursor ready for a value, or just start typing `color` and accept VS Code's native CSS IntelliSense suggestion.

▶ conic-gradient()
  Description: Creates an image consisting of a gradient with color transitions rotated around a center point.
  Syntax:      background: conic-gradient([from angle] [at position], color-stop1, color-stop2, ...);
  VSCode/Emmet: Begin typing `conic-gradient` -- VS Code will suggest the function in background-image contexts.

▶ cursor
  Description: Sets the mouse cursor shown when hovering an element.
  Syntax:      cursor: pointer;
  VSCode/Emmet: Type the Emmet abbreviation `cur` in a CSS rule and press Tab/Enter to expand it to `cursor: ;` with the cursor ready for a value, or just start typing `cursor` and accept VS Code's native CSS IntelliSense suggestion.

▶ filter
  Description: Applies graphical effects like blur or grayscale.
  Syntax:      filter: grayscale(1);
  VSCode/Emmet: Start typing `filter` in a CSS rule -- VS Code's native CSS IntelliSense will suggest the property; press Enter/Tab to accept it (Ctrl+Space to force the suggestion list).

▶ isolation
  Description: Creates a new stacking context, isolating blend modes to within an element.
  Syntax:      isolation: isolate;
  VSCode/Emmet: Start typing `isolation` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ linear-gradient()
  Description: Creates an image consisting of a progressive transition between two or more colors along a straight line.
  Syntax:      background: linear-gradient([angle | to side-corner], color1 [stop1], color2 [stop2], ...);
  VSCode/Emmet: Start typing `linear-gradient` -- VS Code's CSS IntelliSense will auto-suggest it inside background values; press Enter/Tab.

▶ mix-blend-mode
  Description: Sets how an element's content blends with content behind it.
  Syntax:      mix-blend-mode: multiply;
  VSCode/Emmet: Start typing `mix-blend-mode` in a CSS rule -- VS Code's native CSS IntelliSense will suggest it; press Enter/Tab to accept.

▶ object-fit
  Description: Controls how a replaced element (img/video) fits its box.
  Syntax:      object-fit: cover;
  VSCode/Emmet: Start typing `object-fit` in a CSS rule -- VS Code's native CSS IntelliSense will suggest the property; press Enter/Tab to accept it (Ctrl+Space to force the suggestion list).

▶ opacity
  Description: Sets the transparency of an element (0 to 1).
  Syntax:      opacity: 0.75;
  VSCode/Emmet: Type the Emmet abbreviation `op` in a CSS rule and press Tab/Enter to expand it to `opacity: ;` with the cursor ready for a value, or just start typing `opacity` and accept VS Code's native CSS IntelliSense suggestion.

▶ radial-gradient()
  Description: Creates an image consisting of a progressive transition between two or more colors radiating from an origin point.
  Syntax:      background: radial-gradient([shape | size] [at position], color1, color2, ...);
  VSCode/Emmet: Type `radial-gradient` within a background value -- VS Code's IntelliSense will suggest and autocomplete it.

▶ repeating-linear-gradient()
  Description: Creates a repeating linear gradient pattern that tiles infinitely across the background.
  Syntax:      background: repeating-linear-gradient(angle, color1, color1 width1, color2 width1, color2 width2);
  VSCode/Emmet: Type `repeating-linear-gradient` within a background definition; VS Code autocomplete will trigger.

▶ repeating-radial-gradient()
  Description: Creates a repeating radial gradient pattern of concentric rings that tile infinitely.
  Syntax:      background: repeating-radial-gradient(shape, color1, color1 radius1, color2 radius1, color2 radius2);
  VSCode/Emmet: Type `repeating-radial-gradient` -- press Tab/Enter to autocomplete using VS Code's background helpers.


================================================================================
3. JAVASCRIPT REFERENCE INDEX (145 APIs & Methods)
================================================================================

[Category: Advanced Objects] (6 entries)
--------------------------------------------------------------------------------
▶ new Proxy()
  Description: Wraps an object to intercept and customize fundamental operations like get/set.
  Syntax:      const p = new Proxy(target, {
  VSCode/Emmet: Type the built-in VS Code snippet prefix `new` and press Tab/Enter on the suggestion to expand it into a full `new Proxy()` block with tab-stops for each part.

▶ new WeakMap()
  Description: A Map whose keys must be objects and are garbage-collected when unreferenced.
  Syntax:      const cache = new WeakMap();
  VSCode/Emmet: Type the built-in VS Code snippet prefix `new` and press Tab/Enter on the suggestion to expand it into a full `new WeakMap()` block with tab-stops for each part.

▶ new WeakSet()
  Description: A Set of objects that doesn't prevent its members from being garbage-collected.
  Syntax:      const seen = new WeakSet();
  VSCode/Emmet: Type the built-in VS Code snippet prefix `new` and press Tab/Enter on the suggestion to expand it into a full `new WeakSet()` block with tab-stops for each part.

▶ Object.defineProperty()
  Description: Precisely defines a new property or modifies an existing one on an object.
  Syntax:      Object.defineProperty(obj, 'id', { value: 1, writable: false });
  VSCode/Emmet: There is no Emmet-style abbreviation for `Object.defineProperty()`. In VS Code, type the object/value followed by `.`, then choose `Object.defineProperty()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Object.getPrototypeOf()
  Description: Returns the prototype (internal [[Prototype]]) of an object.
  Syntax:      Object.getPrototypeOf([]) === Array.prototype;
  VSCode/Emmet: There is no Emmet-style abbreviation for `Object.getPrototypeOf()`. In VS Code, type the object/value followed by `.`, then choose `Object.getPrototypeOf()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Reflect.get()
  Description: Performs a default object operation (like get) in a way that pairs with Proxy traps.
  Syntax:      Reflect.get(obj, 'name');
  VSCode/Emmet: There is no Emmet-style abbreviation for `Reflect.get()`. In VS Code, type the object/value followed by `.`, then choose `Reflect.get()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.


[Category: Arrays] (25 entries)
--------------------------------------------------------------------------------
▶ [...new Set(arr)]
  Description: De-duplicate an array.
  Syntax:      [...new Set(arr)]
  VSCode/Emmet: Type the built-in VS Code snippet prefix `new` and press Tab/Enter on the suggestion to expand it into a full `[...new Set(arr)]` block with tab-stops for each part.

▶ arr.at()
  Description: Returns the item at an index, supporting negative indices.
  Syntax:      [1,2,3].at(-1); // 3
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.at()`. In VS Code, type the object/value followed by `.`, then choose `arr.at()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.concat()
  Description: Merges two or more arrays into a new array.
  Syntax:      [1,2].concat([3,4]); // [1,2,3,4]
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.concat()`. In VS Code, type the object/value followed by `.`, then choose `arr.concat()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.fill()
  Description: Fills array elements with a static value.
  Syntax:      new Array(3).fill(0); // [0,0,0]
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.fill()`. In VS Code, type the object/value followed by `.`, then choose `arr.fill()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.filter()
  Description: Keep only matching items.
  Syntax:      arr.filter()
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.filter()`. In VS Code, type the object/value followed by `.`, then choose `arr.filter()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.find()
  Description: Get the first matching item.
  Syntax:      arr.find()
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.find()`. In VS Code, type the object/value followed by `.`, then choose `arr.find()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.findIndex()
  Description: Returns the index of the first matching item, or -1.
  Syntax:      [1,2,3].findIndex(n => n>1); // 1
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.findIndex()`. In VS Code, type the object/value followed by `.`, then choose `arr.findIndex()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.flat()
  Description: Flattens nested arrays up to the given depth.
  Syntax:      [1,[2,[3]]].flat(2); // [1,2,3]
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.flat()`. In VS Code, type the object/value followed by `.`, then choose `arr.flat()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.flatMap()
  Description: Maps then flattens the result by one level.
  Syntax:      [1,2].flatMap(n => [n, n*2]); // [1,2,2,4]
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.flatMap()`. In VS Code, type the object/value followed by `.`, then choose `arr.flatMap()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.includes()
  Description: Checks whether an array contains a given value.
  Syntax:      [1,2,3].includes(2); // true
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.includes()`. In VS Code, type the object/value followed by `.`, then choose `arr.includes()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.indexOf()
  Description: Returns the first index of a matching value, or -1.
  Syntax:      [1,2,3].indexOf(2); // 1
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.indexOf()`. In VS Code, type the object/value followed by `.`, then choose `arr.indexOf()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.join()
  Description: Joins all array items into a string with a separator.
  Syntax:      [1,2,3].join('-'); // '1-2-3'
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.join()`. In VS Code, type the object/value followed by `.`, then choose `arr.join()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.map()
  Description: Transform each item into a new array.
  Syntax:      arr.map()
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.map()`. In VS Code, type the object/value followed by `.`, then choose `arr.map()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.pop()
  Description: Removes and returns the last item of an array.
  Syntax:      [1,2,3].pop(); // 3
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.pop()`. In VS Code, type the object/value followed by `.`, then choose `arr.pop()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.push()
  Description: Adds one or more items to the end of an array.
  Syntax:      const arr=[1,2]; arr.push(3); // [1,2,3]
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.push()`. In VS Code, type the object/value followed by `.`, then choose `arr.push()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.reduce()
  Description: Accumulate to a single value.
  Syntax:      arr.reduce()
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.reduce()`. In VS Code, type the object/value followed by `.`, then choose `arr.reduce()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.reverse()
  Description: Reverses the order of array items in place.
  Syntax:      [1,2,3].reverse(); // [3,2,1]
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.reverse()`. In VS Code, type the object/value followed by `.`, then choose `arr.reverse()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.shift()
  Description: Removes and returns the first item of an array.
  Syntax:      [1,2,3].shift(); // 1
  VSCode/Emmet: Type the built-in VS Code snippet prefix `if` and press Tab/Enter on the suggestion to expand it into a full `arr.shift()` block with tab-stops for each part.

▶ arr.slice()
  Description: Returns a shallow copy of a portion of an array.
  Syntax:      [1,2,3,4].slice(1,3); // [2,3]
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.slice()`. In VS Code, type the object/value followed by `.`, then choose `arr.slice()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.some() / every()
  Description: Test if some/all items match.
  Syntax:      arr.some() / every()
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.some() / every()`. In VS Code, type the object/value followed by `.`, then choose `arr.some() / every()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.sort()
  Description: Sorts array items in place.
  Syntax:      [3,1,2].sort((a,b) => a-b); // [1,2,3]
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.sort()`. In VS Code, type the object/value followed by `.`, then choose `arr.sort()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.splice()
  Description: Adds/removes items in place at a given index.
  Syntax:      const a=[1,2,3]; a.splice(1,1,'x'); // [1,'x',3]
  VSCode/Emmet: There is no Emmet-style abbreviation for `arr.splice()`. In VS Code, type the object/value followed by `.`, then choose `arr.splice()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ arr.unshift()
  Description: Adds one or more items to the start of an array.
  Syntax:      [2,3].unshift(1); // [1,2,3]
  VSCode/Emmet: Type the built-in VS Code snippet prefix `if` and press Tab/Enter on the suggestion to expand it into a full `arr.unshift()` block with tab-stops for each part.

▶ Array.from()
  Description: Creates an array from an iterable or array-like value.
  Syntax:      Array.from('abc'); // ['a','b','c']
  VSCode/Emmet: There is no Emmet-style abbreviation for `Array.from()`. In VS Code, type the object/value followed by `.`, then choose `Array.from()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Array.isArray()
  Description: Checks whether a value is an array.
  Syntax:      Array.isArray([1,2]); // true
  VSCode/Emmet: There is no Emmet-style abbreviation for `Array.isArray()`. In VS Code, type the object/value followed by `.`, then choose `Array.isArray()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.


[Category: Async] (2 entries)
--------------------------------------------------------------------------------
▶ async / await
  Description: Write promises like sync code.
  Syntax:      async function getData() {
  VSCode/Emmet: Type the built-in VS Code snippet prefix `async function` and press Tab/Enter on the suggestion to expand it into a full `async / await` block with tab-stops for each part.

▶ Promise.all()
  Description: Wait for multiple promises.
  Syntax:      Promise.all()
  VSCode/Emmet: There is no Emmet-style abbreviation for `Promise.all()`. In VS Code, type the object/value followed by `.`, then choose `Promise.all()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.


[Category: Async & APIs] (8 entries)
--------------------------------------------------------------------------------
▶ fetch()
  Description: Makes an HTTP request and returns a Promise for the response.
  Syntax:      const res = await fetch('/api/data');
  VSCode/Emmet: There is no Emmet-style abbreviation for `fetch()`. In VS Code, type the object/value followed by `.`, then choose `fetch()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ localStorage
  Description: Stores key-value data in the browser persistently.
  Syntax:      localStorage.setItem('theme', 'dark');
  VSCode/Emmet: There is no Emmet-style abbreviation for `localStorage`. In VS Code, type the object/value followed by `.`, then choose `localStorage` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ new Map()
  Description: Creates a key-value collection with any type of key.
  Syntax:      const m = new Map();
  VSCode/Emmet: Type the built-in VS Code snippet prefix `new` and press Tab/Enter on the suggestion to expand it into a full `new Map()` block with tab-stops for each part.

▶ new Set()
  Description: Creates a collection of unique values.
  Syntax:      const s = new Set([1,2,2,3]); // {1,2,3}
  VSCode/Emmet: Type the built-in VS Code snippet prefix `new` and press Tab/Enter on the suggestion to expand it into a full `new Set()` block with tab-stops for each part.

▶ Promise.allSettled()
  Description: Waits for all promises to settle, success or failure.
  Syntax:      const results = await Promise.allSettled([p1, p2]);
  VSCode/Emmet: There is no Emmet-style abbreviation for `Promise.allSettled()`. In VS Code, type the object/value followed by `.`, then choose `Promise.allSettled()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Promise.race()
  Description: Resolves/rejects as soon as the first promise settles.
  Syntax:      Promise.race([p1, p2]).then(first => ...);
  VSCode/Emmet: There is no Emmet-style abbreviation for `Promise.race()`. In VS Code, type the object/value followed by `.`, then choose `Promise.race()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ setInterval()
  Description: Repeatedly runs a function on a fixed delay.
  Syntax:      setInterval(() => tick(), 1000);
  VSCode/Emmet: Type the built-in VS Code snippet prefix `setinterval` and press Tab/Enter on the suggestion to expand it into a full `setInterval()` block with tab-stops for each part.

▶ setTimeout()
  Description: Runs a function once after a delay.
  Syntax:      setTimeout(() => alert('Hi'), 1000);
  VSCode/Emmet: Type the built-in VS Code snippet prefix `settimeout` and press Tab/Enter on the suggestion to expand it into a full `setTimeout()` block with tab-stops for each part.


[Category: Built-ins] (10 entries)
--------------------------------------------------------------------------------
▶ Date.now()
  Description: Returns the current time as milliseconds since the Unix epoch.
  Syntax:      const start = Date.now();
  VSCode/Emmet: There is no Emmet-style abbreviation for `Date.now()`. In VS Code, type the object/value followed by `.`, then choose `Date.now()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ import() (dynamic)
  Description: Loads a module asynchronously at runtime, returning a Promise.
  Syntax:      const mod = await import('./chart.js');
  VSCode/Emmet: Type the built-in VS Code snippet prefix `import` and press Tab/Enter on the suggestion to expand it into a full `import() (dynamic)` block with tab-stops for each part.

▶ new Date().toISOString()
  Description: Formats a Date as an ISO 8601 string, useful for APIs and storage.
  Syntax:      new Date().toISOString(); // '2026-07-11T12:00:00.000Z'
  VSCode/Emmet: Type the built-in VS Code snippet prefix `new` and press Tab/Enter on the suggestion to expand it into a full `new Date().toISOString()` block with tab-stops for each part.

▶ new URL()
  Description: Parses a URL string into its component parts (host, pathname, searchParams...).
  Syntax:      const u = new URL('https://example.com/p?x=1');
  VSCode/Emmet: Type the built-in VS Code snippet prefix `new` and press Tab/Enter on the suggestion to expand it into a full `new URL()` block with tab-stops for each part.

▶ new URLSearchParams()
  Description: Parses and builds URL query strings.
  Syntax:      const params = new URLSearchParams('a=1&b=2');
  VSCode/Emmet: Type the built-in VS Code snippet prefix `new` and press Tab/Enter on the suggestion to expand it into a full `new URLSearchParams()` block with tab-stops for each part.

▶ queueMicrotask()
  Description: Schedules a callback to run in the microtask queue, before the next event loop tick.
  Syntax:      queueMicrotask(() => console.log('after current task'));
  VSCode/Emmet: There is no Emmet-style abbreviation for `queueMicrotask()`. In VS Code, type the object/value followed by `.`, then choose `queueMicrotask()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ regex.exec()
  Description: Executes a regex search and returns match details, or null.
  Syntax:      /(\d+)-(\d+)/.exec('10-20'); // ['10-20','10','20']
  VSCode/Emmet: There is no Emmet-style abbreviation for `regex.exec()`. In VS Code, type the object/value followed by `.`, then choose `regex.exec()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ regex.test()
  Description: Checks whether a string matches a regular expression, returning true/false.
  Syntax:      /^\d+$/.test('123'); // true
  VSCode/Emmet: There is no Emmet-style abbreviation for `regex.test()`. In VS Code, type the object/value followed by `.`, then choose `regex.test()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ requestAnimationFrame()
  Description: Schedules a callback to run before the browser's next repaint.
  Syntax:      requestAnimationFrame(() => { el.style.left = pos + 'px'; });
  VSCode/Emmet: There is no Emmet-style abbreviation for `requestAnimationFrame()`. In VS Code, type the object/value followed by `.`, then choose `requestAnimationFrame()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ sessionStorage
  Description: Stores key-value data in the browser for the duration of the page session.
  Syntax:      sessionStorage.setItem('draft', text);
  VSCode/Emmet: There is no Emmet-style abbreviation for `sessionStorage`. In VS Code, type the object/value followed by `.`, then choose `sessionStorage` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.


[Category: Classes] (4 entries)
--------------------------------------------------------------------------------
▶ class getter/setter
  Description: Defines computed properties on a class using get/set accessor methods.
  Syntax:      class Circle {
  VSCode/Emmet: Type the built-in VS Code snippet prefix `class` and press Tab/Enter on the suggestion to expand it into a full `class getter/setter` block with tab-stops for each part.

▶ extends / super
  Description: Creates a subclass and calls the parent class's constructor/methods.
  Syntax:      class Dog extends Animal {
  VSCode/Emmet: VS Code doesn't ship a single-word Emmet abbreviation for this construct. Start typing it (e.g. the first keyword) and accept the matching suggestion from IntelliSense, or use Insert Snippet (Ctrl+Shift+P > "Insert Snippet") if you've defined a custom one.

▶ private field (#field)
  Description: Declares a class field that is only accessible from within the class.
  Syntax:      class Counter {
  VSCode/Emmet: VS Code doesn't ship a single-word Emmet abbreviation for this construct. Start typing it (e.g. the first keyword) and accept the matching suggestion from IntelliSense, or use Insert Snippet (Ctrl+Shift+P > "Insert Snippet") if you've defined a custom one.

▶ static method
  Description: Defines a method that belongs to the class itself, not its instances.
  Syntax:      class MathUtils {
  VSCode/Emmet: VS Code doesn't ship a single-word Emmet abbreviation for this construct. Start typing it (e.g. the first keyword) and accept the matching suggestion from IntelliSense, or use Insert Snippet (Ctrl+Shift+P > "Insert Snippet") if you've defined a custom one.


[Category: Control Flow] (8 entries)
--------------------------------------------------------------------------------
▶ do...while
  Description: Runs a block once, then repeats while a condition is true.
  Syntax:      do {
  VSCode/Emmet: Type the built-in VS Code snippet prefix `while` and press Tab/Enter on the suggestion to expand it into a full `do...while` block with tab-stops for each part.

▶ for loop
  Description: Repeats a block a fixed number of times using a counter.
  Syntax:      for (let i=0; i<arr.length; i++) {
  VSCode/Emmet: Type the built-in VS Code snippet prefix `for` and press Tab/Enter on the suggestion to expand it into a full `for loop` block with tab-stops for each part.

▶ for...in
  Description: Iterates over the enumerable keys of an object.
  Syntax:      for (const key in obj) {
  VSCode/Emmet: Type the built-in VS Code snippet prefix `for` and press Tab/Enter on the suggestion to expand it into a full `for...in` block with tab-stops for each part.

▶ for...of
  Description: Iterates over the values of an iterable (arrays, strings, Maps...).
  Syntax:      for (const item of items) {
  VSCode/Emmet: Type the built-in VS Code snippet prefix `for` and press Tab/Enter on the suggestion to expand it into a full `for...of` block with tab-stops for each part.

▶ if / else
  Description: Branches execution based on a condition.
  Syntax:      if (age >= 18) {
  VSCode/Emmet: Type the built-in VS Code snippet prefix `if` and press Tab/Enter on the suggestion to expand it into a full `if / else` block with tab-stops for each part.

▶ switch statement
  Description: Branches execution based on matching one of several values.
  Syntax:      switch (status) {
  VSCode/Emmet: Type the built-in VS Code snippet prefix `switch` and press Tab/Enter on the suggestion to expand it into a full `switch statement` block with tab-stops for each part.

▶ try / catch
  Description: Runs code and handles any error it throws.
  Syntax:      try {
  VSCode/Emmet: Type the built-in VS Code snippet prefix `trycatch` and press Tab/Enter on the suggestion to expand it into a full `try / catch` block with tab-stops for each part.

▶ while loop
  Description: Repeats a block while a condition remains true.
  Syntax:      while (count < 5) {
  VSCode/Emmet: Type the built-in VS Code snippet prefix `while` and press Tab/Enter on the suggestion to expand it into a full `while loop` block with tab-stops for each part.


[Category: DOM] (14 entries)
--------------------------------------------------------------------------------
▶ addEventListener()
  Description: Listen for an event.
  Syntax:      addEventListener()
  VSCode/Emmet: There is no Emmet-style abbreviation for `addEventListener()`. In VS Code, type the object/value followed by `.`, then choose `addEventListener()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ appendChild()
  Description: Appends a node as the last child of a parent.
  Syntax:      parent.appendChild(child);
  VSCode/Emmet: There is no Emmet-style abbreviation for `appendChild()`. In VS Code, type the object/value followed by `.`, then choose `appendChild()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ classList.toggle()
  Description: Toggle a CSS class.
  Syntax:      classList.toggle()
  VSCode/Emmet: Type the built-in VS Code snippet prefix `class` and press Tab/Enter on the suggestion to expand it into a full `classList.toggle()` block with tab-stops for each part.

▶ closest()
  Description: Finds the nearest ancestor (or self) matching a selector.
  Syntax:      el.closest('.card');
  VSCode/Emmet: There is no Emmet-style abbreviation for `closest()`. In VS Code, type the object/value followed by `.`, then choose `closest()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ createElement()
  Description: Creates a new element node of the given tag.
  Syntax:      document.createElement('div');
  VSCode/Emmet: There is no Emmet-style abbreviation for `createElement()`. In VS Code, type the object/value followed by `.`, then choose `createElement()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ dataset
  Description: Read data-* attributes.
  Syntax:      dataset()
  VSCode/Emmet: There is no Emmet-style abbreviation for `dataset`. In VS Code, type the object/value followed by `.`, then choose `dataset` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ getElementById()
  Description: Selects a single element by its id attribute.
  Syntax:      document.getElementById('app');
  VSCode/Emmet: There is no Emmet-style abbreviation for `getElementById()`. In VS Code, type the object/value followed by `.`, then choose `getElementById()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ preventDefault()
  Description: Cancels the default action of an event.
  Syntax:      form.addEventListener('submit', e => e.preventDefault());
  VSCode/Emmet: There is no Emmet-style abbreviation for `preventDefault()`. In VS Code, type the object/value followed by `.`, then choose `preventDefault()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ querySelector()
  Description: Select the first matching element.
  Syntax:      querySelector()
  VSCode/Emmet: There is no Emmet-style abbreviation for `querySelector()`. In VS Code, type the object/value followed by `.`, then choose `querySelector()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ querySelectorAll()
  Description: Selects all matching elements as a static NodeList.
  Syntax:      document.querySelectorAll('.item');
  VSCode/Emmet: There is no Emmet-style abbreviation for `querySelectorAll()`. In VS Code, type the object/value followed by `.`, then choose `querySelectorAll()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ removeChild()
  Description: Removes a child node from its parent.
  Syntax:      parent.removeChild(child);
  VSCode/Emmet: There is no Emmet-style abbreviation for `removeChild()`. In VS Code, type the object/value followed by `.`, then choose `removeChild()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ removeEventListener()
  Description: Removes a previously registered event listener.
  Syntax:      btn.removeEventListener('click', handler);
  VSCode/Emmet: There is no Emmet-style abbreviation for `removeEventListener()`. In VS Code, type the object/value followed by `.`, then choose `removeEventListener()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ setAttribute()
  Description: Sets the value of an element's attribute.
  Syntax:      el.setAttribute('data-id', '42');
  VSCode/Emmet: There is no Emmet-style abbreviation for `setAttribute()`. In VS Code, type the object/value followed by `.`, then choose `setAttribute()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ stopPropagation()
  Description: Stops an event from bubbling further up the DOM.
  Syntax:      el.addEventListener('click', e => e.stopPropagation());
  VSCode/Emmet: There is no Emmet-style abbreviation for `stopPropagation()`. In VS Code, type the object/value followed by `.`, then choose `stopPropagation()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.


[Category: ES6+] (6 entries)
--------------------------------------------------------------------------------
▶ arrow fn
  Description: Concise function syntax.
  Syntax:      const add = (a, b) => a + b;
  VSCode/Emmet: There is no Emmet-style abbreviation for `arrow fn`. In VS Code, type the object/value followed by `.`, then choose `arrow fn` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ destructuring
  Description: Unpack values from objects/arrays.
  Syntax:      destructuring()
  VSCode/Emmet: There is no Emmet-style abbreviation for `destructuring`. In VS Code, type the object/value followed by `.`, then choose `destructuring` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ let / const
  Description: Block-scoped variables.
  Syntax:      let / const()
  VSCode/Emmet: There is no Emmet-style abbreviation for `let / const`. In VS Code, type the object/value followed by `.`, then choose `let / const` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ optional chaining
  Description: Safe nested property access.
  Syntax:      optional chaining()
  VSCode/Emmet: There is no Emmet-style abbreviation for `optional chaining`. In VS Code, type the object/value followed by `.`, then choose `optional chaining` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ spread / rest
  Description: Expand or collect values.
  Syntax:      const merged = { ...a, ...b };
  VSCode/Emmet: There is no Emmet-style abbreviation for `spread / rest`. In VS Code, type the object/value followed by `.`, then choose `spread / rest` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ template literal
  Description: String interpolation.
  Syntax:      template literal()
  VSCode/Emmet: There is no Emmet-style abbreviation for `template literal`. In VS Code, type the object/value followed by `.`, then choose `template literal` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.


[Category: Functions & Classes] (11 entries)
--------------------------------------------------------------------------------
▶ class
  Description: Defines a blueprint for creating objects with shared behavior.
  Syntax:      class User {
  VSCode/Emmet: Type the built-in VS Code snippet prefix `class` and press Tab/Enter on the suggestion to expand it into a full `class` block with tab-stops for each part.

▶ closures
  Description: A function that remembers variables from the scope it was created in.
  Syntax:      function makeCounter() {
  VSCode/Emmet: There is no Emmet-style abbreviation for `closures`. In VS Code, type the object/value followed by `.`, then choose `closures` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ constructor
  Description: Special class method run when a new instance is created.
  Syntax:      constructor(name) {
  VSCode/Emmet: Type the built-in VS Code snippet prefix `ctor` and press Tab/Enter on the suggestion to expand it into a full `constructor` block with tab-stops for each part.

▶ default parameters
  Description: Assigns a fallback value to a function parameter when no argument is passed.
  Syntax:      function greet(name = 'Guest') {
  VSCode/Emmet: There is no Emmet-style abbreviation for `default parameters`. In VS Code, type the object/value followed by `.`, then choose `default parameters` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ export
  Description: Exposes a value/function/class for use in other modules.
  Syntax:      export function add(a,b) { return a+b; }
  VSCode/Emmet: There is no Emmet-style abbreviation for `export`. In VS Code, type the object/value followed by `.`, then choose `export` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ function declaration
  Description: Defines a named, reusable block of code.
  Syntax:      function greet(name) {
  VSCode/Emmet: Type the built-in VS Code snippet prefix `function` and press Tab/Enter on the suggestion to expand it into a full `function declaration` block with tab-stops for each part.

▶ IIFE
  Description: An Immediately Invoked Function Expression, used to run code in its own scope right away.
  Syntax:      (function () {
  VSCode/Emmet: Type the built-in VS Code snippet prefix `if` and press Tab/Enter on the suggestion to expand it into a full `IIFE` block with tab-stops for each part.

▶ import statement
  Description: Imports bindings exported from another module.
  Syntax:      import { add } from './math.js';
  VSCode/Emmet: Type the built-in VS Code snippet prefix `import` and press Tab/Enter on the suggestion to expand it into a full `import statement` block with tab-stops for each part.

▶ rest parameters
  Description: Collects any remaining arguments into a real array parameter.
  Syntax:      function sum(...nums) {
  VSCode/Emmet: There is no Emmet-style abbreviation for `rest parameters`. In VS Code, type the object/value followed by `.`, then choose `rest parameters` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ tagged template
  Description: Passes a template literal's parts to a custom function for processing.
  Syntax:      function highlight(strings, ...values) {
  VSCode/Emmet: There is no Emmet-style abbreviation for `tagged template`. In VS Code, type the object/value followed by `.`, then choose `tagged template` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ this
  Description: References the current execution context object.
  Syntax:      class Timer {
  VSCode/Emmet: There is no Emmet-style abbreviation for `this`. In VS Code, type the object/value followed by `.`, then choose `this` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.


[Category: Generators & Symbols] (4 entries)
--------------------------------------------------------------------------------
▶ function*()
  Description: Defines a generator function that can pause/resume execution using yield.
  Syntax:      function* range(n) {
  VSCode/Emmet: Type the built-in VS Code snippet prefix `function` and press Tab/Enter on the suggestion to expand it into a full `function*()` block with tab-stops for each part.

▶ Symbol.iterator
  Description: Defines how an object behaves when iterated with for...of or spread.
  Syntax:      obj[Symbol.iterator] = function* () { yield 1; yield 2; };
  VSCode/Emmet: There is no Emmet-style abbreviation for `Symbol.iterator`. In VS Code, type the object/value followed by `.`, then choose `Symbol.iterator` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Symbol()
  Description: Creates a unique, immutable primitive value often used as an object key.
  Syntax:      const id = Symbol('id');
  VSCode/Emmet: There is no Emmet-style abbreviation for `Symbol()`. In VS Code, type the object/value followed by `.`, then choose `Symbol()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ yield
  Description: Pauses a generator function and returns a value to the caller.
  Syntax:      function* gen() {
  VSCode/Emmet: There is no Emmet-style abbreviation for `yield`. In VS Code, type the object/value followed by `.`, then choose `yield` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.


[Category: JSON] (1 entries)
--------------------------------------------------------------------------------
▶ JSON.stringify() / parse()
  Description: Convert to/from JSON text.
  Syntax:      JSON.stringify() / parse()
  VSCode/Emmet: Type the built-in VS Code snippet prefix `if` and press Tab/Enter on the suggestion to expand it into a full `JSON.stringify() / parse()` block with tab-stops for each part.


[Category: Math & Numbers] (11 entries)
--------------------------------------------------------------------------------
▶ Math.abs()
  Description: Returns the absolute value of a number.
  Syntax:      Math.abs(-7); // 7
  VSCode/Emmet: There is no Emmet-style abbreviation for `Math.abs()`. In VS Code, type the object/value followed by `.`, then choose `Math.abs()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Math.ceil()
  Description: Rounds a number up to the nearest integer.
  Syntax:      Math.ceil(4.1); // 5
  VSCode/Emmet: There is no Emmet-style abbreviation for `Math.ceil()`. In VS Code, type the object/value followed by `.`, then choose `Math.ceil()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Math.floor()
  Description: Rounds a number down to the nearest integer.
  Syntax:      Math.floor(4.9); // 4
  VSCode/Emmet: There is no Emmet-style abbreviation for `Math.floor()`. In VS Code, type the object/value followed by `.`, then choose `Math.floor()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Math.max()
  Description: Returns the largest of the given numbers.
  Syntax:      Math.max(1,5,3); // 5
  VSCode/Emmet: There is no Emmet-style abbreviation for `Math.max()`. In VS Code, type the object/value followed by `.`, then choose `Math.max()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Math.min()
  Description: Returns the smallest of the given numbers.
  Syntax:      Math.min(1,5,3); // 1
  VSCode/Emmet: There is no Emmet-style abbreviation for `Math.min()`. In VS Code, type the object/value followed by `.`, then choose `Math.min()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Math.random()
  Description: Returns a pseudo-random float between 0 (inclusive) and 1.
  Syntax:      Math.random();
  VSCode/Emmet: Type the built-in VS Code snippet prefix `dowhile` and press Tab/Enter on the suggestion to expand it into a full `Math.random()` block with tab-stops for each part.

▶ Math.round()
  Description: Rounds a number to the nearest integer.
  Syntax:      Math.round(4.5); // 5
  VSCode/Emmet: There is no Emmet-style abbreviation for `Math.round()`. In VS Code, type the object/value followed by `.`, then choose `Math.round()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Number.isInteger()
  Description: Checks whether a value is an integer.
  Syntax:      Number.isInteger(4); // true
  VSCode/Emmet: There is no Emmet-style abbreviation for `Number.isInteger()`. In VS Code, type the object/value followed by `.`, then choose `Number.isInteger()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Number.parseFloat()
  Description: Parses a string and returns a floating point number.
  Syntax:      Number.parseFloat('3.14m'); // 3.14
  VSCode/Emmet: There is no Emmet-style abbreviation for `Number.parseFloat()`. In VS Code, type the object/value followed by `.`, then choose `Number.parseFloat()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Number.parseInt()
  Description: Parses a string and returns an integer.
  Syntax:      Number.parseInt('42px'); // 42
  VSCode/Emmet: There is no Emmet-style abbreviation for `Number.parseInt()`. In VS Code, type the object/value followed by `.`, then choose `Number.parseInt()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ toFixed()
  Description: Formats a number using fixed-point notation.
  Syntax:      (3.14159).toFixed(2); // '3.14'
  VSCode/Emmet: There is no Emmet-style abbreviation for `toFixed()`. In VS Code, type the object/value followed by `.`, then choose `toFixed()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.


[Category: Objects] (7 entries)
--------------------------------------------------------------------------------
▶ hasOwnProperty()
  Description: Checks whether an object has its own (non-inherited) property.
  Syntax:      obj.hasOwnProperty('id');
  VSCode/Emmet: There is no Emmet-style abbreviation for `hasOwnProperty()`. In VS Code, type the object/value followed by `.`, then choose `hasOwnProperty()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Object.assign()
  Description: Copies properties from source objects into a target.
  Syntax:      Object.assign({}, a, b);
  VSCode/Emmet: There is no Emmet-style abbreviation for `Object.assign()`. In VS Code, type the object/value followed by `.`, then choose `Object.assign()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Object.entries()
  Description: Get [key, value] pairs.
  Syntax:      Object.entries()
  VSCode/Emmet: There is no Emmet-style abbreviation for `Object.entries()`. In VS Code, type the object/value followed by `.`, then choose `Object.entries()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Object.freeze()
  Description: Prevents an object from being modified.
  Syntax:      Object.freeze(config);
  VSCode/Emmet: There is no Emmet-style abbreviation for `Object.freeze()`. In VS Code, type the object/value followed by `.`, then choose `Object.freeze()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Object.fromEntries()
  Description: Builds an object from an array of [key, value] pairs.
  Syntax:      Object.fromEntries([['a',1]]); // {a:1}
  VSCode/Emmet: There is no Emmet-style abbreviation for `Object.fromEntries()`. In VS Code, type the object/value followed by `.`, then choose `Object.fromEntries()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Object.keys()
  Description: Returns an array of an object's own enumerable keys.
  Syntax:      Object.keys({a:1,b:2}); // ['a','b']
  VSCode/Emmet: There is no Emmet-style abbreviation for `Object.keys()`. In VS Code, type the object/value followed by `.`, then choose `Object.keys()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ Object.values()
  Description: Returns an array of an object's own enumerable values.
  Syntax:      Object.values({a:1,b:2}); // [1,2]
  VSCode/Emmet: There is no Emmet-style abbreviation for `Object.values()`. In VS Code, type the object/value followed by `.`, then choose `Object.values()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.


[Category: Operators] (5 entries)
--------------------------------------------------------------------------------
▶ &&  /  ||
  Description: Logical AND / OR, often used for guards and defaults.
  Syntax:      isValid && submit();
  VSCode/Emmet: There is no Emmet-style abbreviation for `&&  /  ||`. In VS Code, type the object/value followed by `.`, then choose `&&  /  ||` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ === / !==
  Description: Strict equality/inequality without type coercion.
  Syntax:      1 === '1'; // false
  VSCode/Emmet: There is no Emmet-style abbreviation for `=== / !==`. In VS Code, type the object/value followed by `.`, then choose `=== / !==` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ instanceof
  Description: Checks whether an object is an instance of a class.
  Syntax:      err instanceof TypeError;
  VSCode/Emmet: There is no Emmet-style abbreviation for `instanceof`. In VS Code, type the object/value followed by `.`, then choose `instanceof` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ ternary ?:
  Description: Compact inline if/else expression.
  Syntax:      const label = isActive ? 'On' : 'Off';
  VSCode/Emmet: There is no Emmet-style abbreviation for `ternary ?:`. In VS Code, type the object/value followed by `.`, then choose `ternary ?:` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ typeof
  Description: Returns a string describing a value's type.
  Syntax:      typeof 'hi'; // 'string'
  VSCode/Emmet: There is no Emmet-style abbreviation for `typeof`. In VS Code, type the object/value followed by `.`, then choose `typeof` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.


[Category: Patterns] (8 entries)
--------------------------------------------------------------------------------
▶ ?? nullish coalescing
  Description: Default only for null/undefined.
  Syntax:      ?? nullish coalescing()
  VSCode/Emmet: There is no Emmet-style abbreviation for `?? nullish coalescing`. In VS Code, type the object/value followed by `.`, then choose `?? nullish coalescing` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ computed property names
  Description: Uses a dynamic expression as an object key when creating an object literal.
  Syntax:      const key = 'score';
  VSCode/Emmet: There is no Emmet-style abbreviation for `computed property names`. In VS Code, type the object/value followed by `.`, then choose `computed property names` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ debounce()
  Description: Delay a function until input settles.
  Syntax:      debounce()
  VSCode/Emmet: There is no Emmet-style abbreviation for `debounce()`. In VS Code, type the object/value followed by `.`, then choose `debounce()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ event delegation
  Description: Attaching one listener to a parent to handle events from its current and future children.
  Syntax:      list.addEventListener('click', (e) => {
  VSCode/Emmet: There is no Emmet-style abbreviation for `event delegation`. In VS Code, type the object/value followed by `.`, then choose `event delegation` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ hoisting
  Description: JavaScript's behavior of moving variable/function declarations to the top of their scope.
  Syntax:      console.log(x); // undefined, not an error
  VSCode/Emmet: There is no Emmet-style abbreviation for `hoisting`. In VS Code, type the object/value followed by `.`, then choose `hoisting` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ nested destructuring
  Description: Extracts values from nested objects/arrays in a single statement.
  Syntax:      const { user: { name, address: { city } } } = data;
  VSCode/Emmet: There is no Emmet-style abbreviation for `nested destructuring`. In VS Code, type the object/value followed by `.`, then choose `nested destructuring` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ structuredClone()
  Description: Deep clone any value.
  Syntax:      structuredClone()
  VSCode/Emmet: There is no Emmet-style abbreviation for `structuredClone()`. In VS Code, type the object/value followed by `.`, then choose `structuredClone()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ throttle()
  Description: Limit call frequency.
  Syntax:      throttle()
  VSCode/Emmet: There is no Emmet-style abbreviation for `throttle()`. In VS Code, type the object/value followed by `.`, then choose `throttle()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.


[Category: Strings] (15 entries)
--------------------------------------------------------------------------------
▶ str.charAt()
  Description: Returns the character at a given index.
  Syntax:      'abc'.charAt(1); // 'b'
  VSCode/Emmet: There is no Emmet-style abbreviation for `str.charAt()`. In VS Code, type the object/value followed by `.`, then choose `str.charAt()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ str.endsWith()
  Description: Checks whether a string ends with a given substring.
  Syntax:      'hello'.endsWith('lo'); // true
  VSCode/Emmet: There is no Emmet-style abbreviation for `str.endsWith()`. In VS Code, type the object/value followed by `.`, then choose `str.endsWith()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ str.includes()
  Description: Check for a substring.
  Syntax:      str.includes()
  VSCode/Emmet: There is no Emmet-style abbreviation for `str.includes()`. In VS Code, type the object/value followed by `.`, then choose `str.includes()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ str.indexOf()
  Description: Returns the index of the first occurrence of a substring.
  Syntax:      'hello'.indexOf('l'); // 2
  VSCode/Emmet: There is no Emmet-style abbreviation for `str.indexOf()`. In VS Code, type the object/value followed by `.`, then choose `str.indexOf()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ str.match()
  Description: Matches a string against a regular expression.
  Syntax:      'a1b2'.match(/\d/g); // ['1','2']
  VSCode/Emmet: There is no Emmet-style abbreviation for `str.match()`. In VS Code, type the object/value followed by `.`, then choose `str.match()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ str.padStart()
  Description: Pad a string to a length.
  Syntax:      str.padStart()
  VSCode/Emmet: There is no Emmet-style abbreviation for `str.padStart()`. In VS Code, type the object/value followed by `.`, then choose `str.padStart()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ str.repeat()
  Description: Repeats a string a given number of times.
  Syntax:      'ab'.repeat(3); // 'ababab'
  VSCode/Emmet: There is no Emmet-style abbreviation for `str.repeat()`. In VS Code, type the object/value followed by `.`, then choose `str.repeat()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ str.replace()
  Description: Replaces the first (or all, with /g) match in a string.
  Syntax:      'foo bar'.replace('foo','baz'); // 'baz bar'
  VSCode/Emmet: There is no Emmet-style abbreviation for `str.replace()`. In VS Code, type the object/value followed by `.`, then choose `str.replace()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ str.replaceAll()
  Description: Replace every occurrence.
  Syntax:      str.replaceAll()
  VSCode/Emmet: There is no Emmet-style abbreviation for `str.replaceAll()`. In VS Code, type the object/value followed by `.`, then choose `str.replaceAll()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ str.slice()
  Description: Extracts a section of a string as a new string.
  Syntax:      'hello'.slice(1,3); // 'el'
  VSCode/Emmet: There is no Emmet-style abbreviation for `str.slice()`. In VS Code, type the object/value followed by `.`, then choose `str.slice()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ str.split()
  Description: Splits a string into an array of substrings.
  Syntax:      'a,b,c'.split(','); // ['a','b','c']
  VSCode/Emmet: There is no Emmet-style abbreviation for `str.split()`. In VS Code, type the object/value followed by `.`, then choose `str.split()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ str.startsWith()
  Description: Checks whether a string starts with a given substring.
  Syntax:      'hello'.startsWith('he'); // true
  VSCode/Emmet: There is no Emmet-style abbreviation for `str.startsWith()`. In VS Code, type the object/value followed by `.`, then choose `str.startsWith()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ str.toLowerCase()
  Description: Converts a string to lower case.
  Syntax:      'ABC'.toLowerCase(); // 'abc'
  VSCode/Emmet: There is no Emmet-style abbreviation for `str.toLowerCase()`. In VS Code, type the object/value followed by `.`, then choose `str.toLowerCase()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ str.toUpperCase()
  Description: Converts a string to upper case.
  Syntax:      'abc'.toUpperCase(); // 'ABC'
  VSCode/Emmet: There is no Emmet-style abbreviation for `str.toUpperCase()`. In VS Code, type the object/value followed by `.`, then choose `str.toUpperCase()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

▶ str.trim()
  Description: Removes whitespace from both ends of a string.
  Syntax:      '  hi  '.trim(); // 'hi'
  VSCode/Emmet: There is no Emmet-style abbreviation for `str.trim()`. In VS Code, type the object/value followed by `.`, then choose `str.trim()` from the IntelliSense suggestion list (powered by the built-in TypeScript/JavaScript language service) and press Enter/Tab to insert it.

